# Cosmoteer.Ships.Networks.PartNetworkResourceStoreRules

Opens an existing resource storage on the part to the network so other parts can draw from it or push into it, with optional reserved and maximum levels.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ResourceType
`→ ResourceRules` · required

The resource the store trades on the part network. Exposed as the store's sink and source ResourceType, so it only provides and receives this one resource type through the network.

## ResourceStorage
`→ PartComponentRules` · required

The backing resource storage that the store will use to provide and receive resources.

## ReservedResourceLevel
`float` · optional

The amount of resources in the ResourceStorage that are unavailable to the store. Represents a percentage of max capacity if ResourceLevelsAreFractions is true.

## MaxResourceLevel
`float` · optional

The resource level in the ResourceStorage that the store is able to deposit up to. Represents a percentage of max capacity if ResourceLevelsAreFractions is true.

## ResourceLevelsAreFractions
`bool` · optional

Whether the resource level values are a fraction of the ResourceStorage's capacity, or an absolute value.

## AllowProviding
`bool` · optional · default `true`

Whether the store can fulfill requests for its resource from the part-network.

## AllowReceiving
`bool` · optional · default `true`

Whether the store can receive resources pushed through the part-network.

## Categories
`→ PartNetworkCategory[]` · optional

The network categories of the storage.
