# Halfling.Particles.Updaters.ParticleSetRandomVelocityFromPoint

Launches each new particle straight away from `AwayFromPoint` at a random `Speed`, for outward bursts and shockwaves.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## LocationIn
`ParticleDataID` · optional

The data that stores the particle locations.

## VelocityOut
`ParticleDataID` · optional

The data that stores the particle velocities. This data will be modified.

## Speed
`range<float>` · optional

The initial speed of particles as a random range.

## AwayFromPoint
`Vector2` · optional

The point away from which particles will fly.

## ConvertPointToWorldSpace
`bool` · optional

Whether the AwayFromPoint should be converted to world-space before the velocity is calculated.
