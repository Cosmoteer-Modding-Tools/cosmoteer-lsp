# Cosmoteer.Generators.Ships.Stages.GridStage

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Part
`→ PartRules` · optional

The type of part to add.

## MinColumns
`int` · optional · default `1`

The minimum number of columns to generate.

## MaxColumns
`int` · optional · default `2`

The maximum number of columns to generate.

## MinRows
`int` · optional · default `1`

The minimum number of rows to generate.

## MaxRows
`int` · optional · default `4`

The maximum number of rows to generate.

## MinWidth
`int` · optional · default `2`

The minimum width of each cell.

## MaxWidth
`int` · optional · default `6`

The maximum width of each cell.

## MinHeight
`int` · optional · default `4`

The minimum height of each cell.

## MaxHeight
`int` · optional · default `8`

The maximum height of each cell.

## UniformCellSize
`bool` · optional

Whether or not each cell is of the same size.
