# Halfling.Gui.Components.Text.WidgetTextClipboardController

A widget component that allows the user to copy and paste text to and from the operating system's clipboard.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## AllowCopy
`bool` · optional

Whether copying text is allowed.

## AllowPaste
`bool` · optional

Whether pasting text is allowed.

## CopyHotkey
`enum ViKey[]` · optional

The IInputButton that can be used to copy the selected text to the clipboard.

## PasteHotkey
`enum ViKey[]` · optional

The IInputButton that can be used to paste text from clipboard.
