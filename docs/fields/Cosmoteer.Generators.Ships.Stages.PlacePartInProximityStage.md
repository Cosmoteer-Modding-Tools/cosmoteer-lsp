# Cosmoteer.Generators.Ships.Stages.PlacePartInProximityStage

Places a single kind of part in proximity to other kinds of parts.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## SearchRange
`int` · optional · default `15`

The maximum distance from a potential new part location that an existing part can be and still count as being "near" the potential location.

## PlaceNear
`ProximatePart[]` · optional

The parts near which the new parts should be placed.

## Avoid
`→ PartRules[]` · optional

Avoids putting additional parts near parts of these types.
