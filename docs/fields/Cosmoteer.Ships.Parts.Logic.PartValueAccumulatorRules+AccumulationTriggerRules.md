# Cosmoteer.Ships.Parts.Logic.PartValueAccumulatorRules/AccumulationTriggerRules

One trigger that changes the accumulated value, with the amount, the operation and the states in which it may apply.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Trigger
`ComponentTriggerReferenceRules` · required

The IPartComponentTrigger that triggers this accumulation instance.

## Value
`number` · required

The value to use for accumulating, dissipating, or setting on being triggered.

## Operation
`enum TriggerOperation` · optional · one of: `Accumulate`, `Dissipate`, `Set`, `MultiplyFromBase`

Whether this trigger acts as accumulation, dissipation, or directly sets the value.

## AllowWhenOn
`bool` · optional · default `true`

Whether this trigger activates when the accumulator is on.

## AllowWhenOff
`bool` · optional · default `true`

Whether this trigger activates when the accumulator is off.

## AllowWhenOperational
`bool` · optional · default `true`

Whether this trigger activates when the accumulator is operational.

## AllowWhenNonOperational
`bool` · optional · default `true`

Whether this trigger activates when the accumulator is non-operational.
