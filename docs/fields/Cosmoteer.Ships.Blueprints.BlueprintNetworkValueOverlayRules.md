# Cosmoteer.Ships.Blueprints.BlueprintNetworkValueOverlayRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## BorderColor
`Color` · optional

Color of the border drawn as four segments around the part's rect when the overlay is active. If both this and [[#FillColor]] are unset, no highlight box is drawn (only the value text).

## FillColor
`Color` · optional

Color of the solid quad that fills the part's rect when the overlay is active. Combines with [[#BorderColor]] for a filled-and-outlined highlight.

## ValueSource
`→ PartComponentRules` · required

ID of the component (an `IComponentValueProvider`) on this part whose numeric value is read and displayed. Its value is multiplied by [[#ValueScale]] then formatted with [[#FormatKey]].

## FormatKey
`string` · required

Localization key passed to the string formatter along with the scaled value to produce the displayed text, letting the number be labeled and unit-formatted.

## ValueScale
`float` · optional · default `1`

Multiplier applied to [[#ValueSource]]'s raw value before it is formatted and shown, for converting internal units into display units.

## ValidSignals
`→ BlueprintNetworkOverlaySignalRules[]` · optional

The collection of signal IDs that can activate the value overlay.

## AllowUpstreamSignals
`bool` · optional

Whether signals upstream from the SignalThroughNode subnetwork are valid.

## AllowDownstreamSignals
`bool` · optional · default `true`

Whether signals downstream from (visible to) the SignalThroughNode subnetwork are valid.

## SignalThroughNode
`→ PartComponentRules` · required

ID of the blueprint-available component to receive the network signal from.

## DrawOnHover
`bool` · optional

Whether this value overlay should be drawn regardless of signal when the part is hovered in build mode.
