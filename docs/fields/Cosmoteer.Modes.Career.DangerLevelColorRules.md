# Cosmoteer.Modes.Career.DangerLevelColorRules

Stores rules for how a danger level's color should be determined.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Tier
`int` · required

The danger tier this stop of the color ramp is pinned to. Entries have to be listed in ascending tier order, since the game takes the first entry whose tier is at least the tier being colored. Vanilla pins only tiers 1 and 18.

## Color
`Color` · required

The color used at this stop. Tiers between two stops are blended in HSVA space, tiers below the first stop take its color outright, and tiers above the last stop take the last color. Vanilla runs green at tier 1 to red at tier 18.
