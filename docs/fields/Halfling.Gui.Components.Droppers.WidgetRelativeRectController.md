# Halfling.Gui.Components.Droppers.WidgetRelativeRectController

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## RestrictAutoSizeBounds
`bool` · optional

Whether the attached widget's auto-size bounds will be automatically restricted to its parent's boundaries.

## SourceCorner
`enum Corner` · optional · one of: `TopLeft`, `TopRight`, `BottomLeft`, `BottomRight`

When AutoPosition is true, gets or sets the corner of the source rect to which the attached widget's TargetCorner will be aligned.

## TargetCorner
`enum Corner` · optional · one of: `TopLeft`, `TopRight`, `BottomLeft`, `BottomRight`

When AutoPosition is true, gets or sets the corner of the attached Widget that will be aligned with the source rect's SourceCorner.

## PositionOffset
`Vector2` · optional

When AutoPosition is true, gets or sets the offset to apply to the position of the attached widget once it is aligned with the source rect.

## PreferredHorizontalSpace
`float` · optional

The amount of horizontal space that is preferred when positioning the attached widget. If the amount of space available is less than this value, then the target may be located on the opposite side of the source rect if there is more space there.

## PreferredVerticalSpace
`float` · optional

The amount of vertical space that is preferred when the positioning the attached widget. If the amount of space available is less than this value, then the target may be located on the opposite side of the source rect if there is more space there.

## MatchWidth
`bool` · optional

When AutoPosition is true, gets or sets whether the width of the attached widget will be modified to match the width of the source rect.

## MatchHeight
`bool` · optional

When AutoPosition is true, gets or sets whether the height of the attached widget wwill be modified to match the height of the source rect.
