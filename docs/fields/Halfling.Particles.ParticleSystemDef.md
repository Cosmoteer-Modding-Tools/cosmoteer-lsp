# Halfling.Particles.ParticleSystemDef

The values each particle starts with, what updates them every frame, and how they are drawn. Many effects can share one through `Def`.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## EmitPerSecond
`float` · optional

The number of particles that are spawned every second.

## EmitPerOneShot
`int` · optional

The number of particles that are spawned every time EmitOneShot() is called.

## Initializers
`IParticleDataUpdater[]` · optional

A modifiable list containing the objects used to initialize particle data.

## Updaters
`IParticleDataUpdater[]` · optional

A modifiable list containing the objects used to update particle data.

## Renderer
`IParticleRenderer` · optional

The object that renders the actual particles.

## Material
`Material` · optional

The material used to render the particles.

## DeleteMode
`enum ParticleDeleteMode` · optional · one of: `Fast`, `KeepOrder`

The method used to delete particles.

## FpsCompensation
`enum FpsCompensationMode` · optional · one of: `None`, `Uniform`, `Random`

The method used to compensate for FPS during emission.

## FpsCompensationStrength
`float` · optional

The strength of the FPS compensation.

## EnableMultithreading
`bool` · optional

Whether the particle system will run on multiple threads.

## InitCapacity
`int` · optional

The initial number of particle that the particle system can store before needing to be resized.
