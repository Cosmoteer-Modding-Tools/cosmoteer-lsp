# Halfling.Audio.ISoundEffect

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Sounds
`asset (sound)[]` · optional

A collection Sound objects from which a random Sound will be selected to play.

## VolumeRange
`range<float>` · optional

The range of volumes in which the actual volume of the sound effect will be chosen at random.

## SpeedRange
`range<float>` · optional

The range of speeds in which the actual speed of the sound effect will be chosen at random.

## StartRange
`range<Time>` · optional

The range of possible start times in which the actual start time will be chosen at random.

## Filter
`FilterParams` · optional

The filter, if any, to apply to the sound.
