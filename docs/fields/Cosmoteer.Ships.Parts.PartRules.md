# Cosmoteer.Ships.Parts.PartRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## NameKey
`string` · required

The part's name, displayed when it is selected or hovered over.

## IconNameKey
`string` · optional

The part name as it shows up in the ship editor's menus.

## JobsNameKey
`string` · optional

<!-- TODO: needs documentation -->

## ID
`→ PartRules` · required

Game-wide unique ID of the part. Must start with the author's name.

## OtherIDs
`→ PartRules[]` · optional

Alternate part identifiers this part also answers to.

## FlipWhenLoadingIDs
`→ PartRules[]` · optional

Part IDs that trigger flipping when the ship is loaded.

## DontStackInEditorGroups
`→ EditorGroupRules[]` · optional

<!-- TODO: needs documentation -->

## EditorParentParts
`EditorParentPart[]` · optional

The IDs of parts that will contain this part as a 'sub-part' within the ship editor.

## DefaultEditorHotkey
`enum ViKey[]` · optional

<!-- TODO: needs documentation -->

## EditorReplacementPartID
`→ PartRules` · required

<!-- TODO: needs documentation -->

## DescriptionKey
`string` · optional

<!-- TODO: needs documentation -->

## Resources
`[→ ResourceRules, int][]` · optional

Resource name and cost pairs defining what the part costs to build.

## IsBuildable
`bool` · optional · default `true`

If false, the part cannot be built.

## FractionalCostToRepair
`float` · required

How much of the build cost, at most, is spent to repair the part to full health.

## Size
`IntVector2` · required

The width and height of the part, in tiles.

## PhysicalRect
`IntRect` · optional

<!-- TODO: needs documentation -->

## SaveRect
`IntRect` · optional

<!-- TODO: needs documentation -->

## AllowedContiguity
`enum AdjacencyFlags` · optional · default `Sides` · one of: `None`, `TopLeft`, `Top`, `TopRight`, `Right`, `BottomRight`, `Bottom`, `BottomLeft`, `Left`, `Sides`, `Corners`, `All`

<!-- TODO: needs documentation -->

## GenerateRectCollider
`bool` · optional · default `true`

<!-- TODO: needs documentation -->

## RectColliderRectType
`enum PartRectType` · optional · default `Physical` · one of: `Normal`, `Physical`

<!-- TODO: needs documentation -->

## BoundingRectType
`enum PartRectType` · optional · one of: `Normal`, `Physical`

<!-- TODO: needs documentation -->

## PartNetworkOverlayMidpoint
`Vector2` · optional

<!-- TODO: needs documentation -->

## CellOccupancyFactor
`float` · optional · default `1`

The value by which to multiply the calculated collider occupancy value of cells within the part.

## SelectionPriority
`int` · required

Determines selection ordering when parts overlap.

## SelectionTypeID
`→ PartRules` · optional

Part ID used to group parts together for selection.

## SelectionTypeByRotation
`bool` · required

If true, double-clicking the part in-game only selects parts of the same type facing the same direction.

## SelectionTypeRotations
`int[]` · optional

<!-- TODO: needs documentation -->

## IgnoreRotationForMirroredSelection
`bool` · optional

<!-- TODO: needs documentation -->

## Density
`float` · required

How much one tile of the part weighs, in tons.

## MaxHealth
`int` · required

How much damage the part can endure before being destroyed.

## HealthType
`enum HealthType` · required · one of: `Operational`, `Structural`

The part's health category. See the HealthTypes reference.

## ConstructionWork
`float` · optional

<!-- TODO: needs documentation -->

## ConstructionSwapDelay
`Time` · optional

<!-- TODO: needs documentation -->

## WorkPerRepairedHealth
`float` · optional

<!-- TODO: needs documentation -->

## SalvageProgressMediaEffects
`MultiMediaEffectRules` · optional

<!-- TODO: needs documentation -->

## SalvageProgressMediaEffectsTimeout
`Time` · optional

<!-- TODO: needs documentation -->

## ConstructionProgressMediaEffects
`MultiMediaEffectRules` · optional

<!-- TODO: needs documentation -->

## ConstructionProgressMediaEffectsTimeout
`Time` · optional

<!-- TODO: needs documentation -->

## ConstructionFinishedMediaEffects
`MultiMediaEffectRules` · optional

<!-- TODO: needs documentation -->

## ExplosiveDamageAbsorption
`range<number>` · required

<!-- TODO: needs documentation -->

## DamageResistances
`map<→ DamageType, range<number>>` · optional

<!-- TODO: needs documentation -->

## StatusResistances
`map<→ StatusType, StatusResistance>` · optional

The part's base resistances to status types, interpolated by the current DamageFraction.

## UnderlyingPart
`→ PartRules` · optional

<!-- TODO: needs documentation -->

## UnderlyingPartPerTile
`→ PartRules` · optional

<!-- TODO: needs documentation -->

## CreateUnderlyingWhenDestroyed
`bool` · optional · default `true`

<!-- TODO: needs documentation -->

## TempConstructionPartPerTile
`→ PartRules` · optional

<!-- TODO: needs documentation -->

## CreatePartPerTileWhenGrabbed
`→ PartRules` · optional

<!-- TODO: needs documentation -->

## DestroyedPartHealthFraction
`float` · optional · default `1`

<!-- TODO: needs documentation -->

## CrewKillChanceWhenDestroyed
`float` · required

<!-- TODO: needs documentation -->

## CrewThrowSpeedWhenDestroyed
`float` · required

<!-- TODO: needs documentation -->

## InitialPenetrationResistance
`range<number>` · required

<!-- TODO: needs documentation -->

## ContinuingPenetrationResistance
`range<number>` · required

<!-- TODO: needs documentation -->

## CrewSpeedFactor
`DirectionalCrewSpeeds` · required

The speed at which crew move within the part.

## CrewCongestedSpeedFactor
`DirectionalCrewSpeeds` · required

The speed at which crew move within the part while passing other crew.

## CrewMinimumSpeedFactor
`DirectionalCrewSpeeds` · required

The minimum values to multiply crew speed by within the part.

## FireDamageFactor
`number` · optional

<!-- TODO: needs documentation -->

## IsCrewSalvageable
`bool` · optional · default `true`

<!-- TODO: needs documentation -->

## HasNonContiguousPathing
`bool` · optional

Set to true if some tiles of the part cannot be walked across by crew in a given direction.

## BlockedTravelCells
`IntVector2[]` · optional

<!-- TODO: needs documentation -->

## BlockedTravelCellDirections
`map<IntVector2, enum TravelDirection[]>` · optional

<!-- TODO: needs documentation -->

## ForceManhattanPathInPart
`bool` · optional

<!-- TODO: needs documentation -->

## ProhibitRects
`[→ ProhibitCategory, IntRect][]` · optional

<!-- TODO: needs documentation -->

## ProhibitedBy
`→ ProhibitCategory[]` · optional

<!-- TODO: needs documentation -->

## ContiguityOriginPriority
`int` · optional

<!-- TODO: needs documentation -->

## IsExternal
`bool` · required

If false, the part must have either no doors to anything else or only doors with legal placement.

## IsRotateable
`bool` · required

Whether the part can be rotated during placement.

## IsFlippable
`bool` · required

Whether the part can be flipped during placement.

## IsWalled
`bool` · required

If false, the part can only have doors to parts that are themselves not walled.

## IsSelfDestructible
`bool` · required

<!-- TODO: needs documentation -->

## IsLowPriorityDeconstruction
`bool` · optional

<!-- TODO: needs documentation -->

## ForceBrushCompatibility
`bool` · optional

<!-- TODO: needs documentation -->

## AllowedDoorLocations
`IntVector2[]` · optional

<!-- TODO: needs documentation -->

## MaxDoors
`int` · optional · default `2147483647`

<!-- TODO: needs documentation -->

## GeneratorRequiresDoor
`bool` · required

<!-- TODO: needs documentation -->

## AutoDoorMaxPathLength
`int` · required

<!-- TODO: needs documentation -->

## NoAutoDoors
`bool` · optional

<!-- TODO: needs documentation -->

## DoorAccessWarningLevel
`enum AccessWarningLevel` · optional · default `Strong` · one of: `None`, `Weak`, `Strong`

<!-- TODO: needs documentation -->

## ExternalWalls
`enum AdjacencyFlags` · required · one of: `None`, `TopLeft`, `Top`, `TopRight`, `Right`, `BottomRight`, `Bottom`, `BottomLeft`, `Left`, `Sides`, `Corners`, `All`

The sides of the part that count as solid hull. A wall sprite renders against each listed side from the neighboring cell whenever that cell is empty space or an open side of another part (see [[Cosmoteer.Ships.Parts.PartRules.InternalWalls]]). Directions are in the part's un-rotated orientation and rotate and flip with it. Inherited as `[All]` from `base_part.rules`, so a part that says nothing is walled on every side.

## BlueprintExternalWalls
`enum AdjacencyFlags` · optional · one of: `None`, `TopLeft`, `Top`, `TopRight`, `Right`, `BottomRight`, `Bottom`, `BottomLeft`, `Left`, `Sides`, `Corners`, `All`

The blueprint-view counterpart of [[Cosmoteer.Ships.Parts.PartRules.ExternalWalls]]. When omitted, the blueprint uses `ExternalWalls`.

## ExternalWallsByCell
`map<IntVector2, enum AdjacencyFlags>` · optional

Per-cell override of [[Cosmoteer.Ships.Parts.PartRules.ExternalWalls]] for multi-cell parts. Each `Key` is a cell of the part (`[0, 0]` is the top-left cell) and each `Value` lists the sides of that cell that count as solid hull, with `[]` meaning no walls there. Cells without an entry keep the whole-part `ExternalWalls` value.

## BlueprintExternalWallsByCell
`map<IntVector2, enum AdjacencyFlags>` · optional

The blueprint-view counterpart of [[Cosmoteer.Ships.Parts.PartRules.ExternalWallsByCell]]. When omitted, the blueprint uses `ExternalWallsByCell`.

## InternalWalls
`enum AdjacencyFlags` · required · one of: `None`, `TopLeft`, `Top`, `TopRight`, `Right`, `BottomRight`, `Bottom`, `BottomLeft`, `Left`, `Sides`, `Corners`, `All`

The sides of the part's cells that are visually open, so a wall may render inside this part's own cell along them. A wall appears on a listed side only when the neighbor on that side declares the shared edge solid via its [[Cosmoteer.Ships.Parts.PartRules.ExternalWalls]]. Inherited as `[None]` from `base_part.rules` (fully enclosed room). Angled armor lists its open wedge sides here, and open-platform structure uses `[All]`.

## BlueprintInternalWalls
`enum AdjacencyFlags` · optional · one of: `None`, `TopLeft`, `Top`, `TopRight`, `Right`, `BottomRight`, `Bottom`, `BottomLeft`, `Left`, `Sides`, `Corners`, `All`

The blueprint-view counterpart of [[Cosmoteer.Ships.Parts.PartRules.InternalWalls]]. When omitted, the blueprint uses `InternalWalls`.

## InternalWallsByCell
`map<IntVector2, enum AdjacencyFlags>` · optional

Per-cell override of [[Cosmoteer.Ships.Parts.PartRules.InternalWalls]] for multi-cell parts, in the same `Key`/`Value` form as [[Cosmoteer.Ships.Parts.PartRules.ExternalWallsByCell]]. Cells without an entry keep the whole-part `InternalWalls` value.

## BlueprintInternalWallsByCell
`map<IntVector2, enum AdjacencyFlags>` · optional

The blueprint-view counterpart of [[Cosmoteer.Ships.Parts.PartRules.InternalWallsByCell]]. When omitted, the blueprint uses `InternalWallsByCell`.

## VirtualInternalCells
`VirtualInternalCell[]` · optional

<!-- TODO: needs documentation -->

## TypeCategories
`→ PartCategory[]` · optional

Part-category identifiers used to classify the part.

## RequiresCategories
`→ PartCategory[]` · optional

<!-- TODO: needs documentation -->

## AffectsValidityOfCategories
`→ PartCategory[]` · optional

<!-- TODO: needs documentation -->

## SignificanceToggle
`→ PartComponentRules` · optional

ID of the IPartComponentToggle that determines whether this part should be considered for determining ship 'significance'.

## EditorIcon
`Sprite` · required

<!-- TODO: needs documentation -->

## JobsIcon
`Sprite` · optional

Sprite shown for the part's job assignments.

## FlipHRotate
`int[]` · optional

The rotation mapping applied when the part is flipped horizontally.

## FlipVRotate
`int[]` · optional

The rotation mapping applied when the part is flipped vertically.

## AIValueFactor
`float` · optional · default `1`

How much the AI wants to destroy this part on ships it considers enemies.

## ReceivableBuffs
`→ BuffType[]` · optional

<!-- TODO: needs documentation -->

## MinBuffValues
`map<→ BuffType, float>` · optional

Per-buff minimum values this part clamps received buffs to.

## MaxBuffValues
`map<→ BuffType, float>` · optional

Per-buff maximum values this part clamps received buffs to.

## Stats
`map<→ PartStatRules, PartStatValues>` · optional

<!-- TODO: needs documentation -->

## Features
`→ PartFeature[]` · optional

<!-- TODO: needs documentation -->

## StatsByCategory
`PartStatsCategory[]` · optional

<!-- TODO: needs documentation -->

## StatsNotesKey
`string` · optional

<!-- TODO: needs documentation -->

## SecondaryToolTip
`PartToolTipData` · optional

<!-- TODO: needs documentation -->

## FractionalNonPlayerRefund
`float` · optional

<!-- TODO: needs documentation -->

## EditorGroups
`→ EditorGroupRules[]` · optional

The names of the ship-editor tabs the part appears in.

## EditorGroup
`→ EditorGroupRules` · optional

The name of the ship-editor tab the part appears in.

## ExplosiveDamageResistance
`number` · optional

<!-- TODO: needs documentation -->

## Prohibits
`→ ProhibitCategory[]` · optional

<!-- TODO: needs documentation -->

## ProhibitLeft
`int` · optional

<!-- TODO: needs documentation -->

## ProhibitRight
`int` · optional

<!-- TODO: needs documentation -->

## ProhibitAbove
`int` · optional

<!-- TODO: needs documentation -->

## ProhibitBelow
`int` · optional

<!-- TODO: needs documentation -->
