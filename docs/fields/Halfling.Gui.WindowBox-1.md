# Halfling.Gui.WindowBox`1

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## TitleBox
`LayoutBox` · optional

The LayoutBox that contains the title bar, title, and window buttons.

## TitleLabel
`Label` · optional

The Label that displays the title text in the TitleBox.

## CloseButton
`ImageButton` · optional

The Button that can be used to close this WindowBox.

## DragController
`WidgetDragController` · optional

The settings for the component that controls dragging this WindowBox via its title bar.

## ResizeController
`WidgetResizeController` · optional

The settings for the component that controls resizing this WindowBox via its edges.

## RaiseController
`WidgetRaiseController` · optional

The settings for the component that controls when and whether this WindowBox is raised to the front of its parent.

## TitleText
`string` · optional

The text to display on the TitleLabel in the TitleBox. This is synonymous with TitleLabel.TextRenderer.Text.

## TitleTextProvider
`ITextProvider` · optional

An ITextProvider object that, if not null, will keep this WindowBox's TitleText property updated to match the ITextProvider.

## AutoClose
`bool` · optional

Whether this WindowBox should be automatically closed when its CloseButton is clicked.
