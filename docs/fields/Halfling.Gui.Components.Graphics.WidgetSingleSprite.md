# Halfling.Gui.Components.Graphics.WidgetSingleSprite

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## DesiredSize
`Vector2` · optional

The desired size of the sprite, which may then be scaled depending on the ScaleMode property.

## ScaleMode
`enum SpriteScaleMode` · optional · one of: `None`, `StretchNoAspect`, `FitMaintainAspect`, `ShrinkFitMaintainAspect`, `FillMaintainAspect`

Whether and how the sprite will be stretched or scaled to match the size of its containing rect.

## Clipped
`bool` · optional

Whether any portions of the sprite outside the container rect will be clipped.

## Padding
`Borders` · optional

The empty space padding around the sprite.
