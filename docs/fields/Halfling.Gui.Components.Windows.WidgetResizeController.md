# Halfling.Gui.Components.Windows.WidgetResizeController

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## MouseButton
`enum ViKey[]` · optional

The mouse IInputButton to which the edge drag will respond.

## AllowedEdges
`enum Edges` · optional · one of: `None`, `Left`, `Right`, `Top`, `Bottom`, `All`

A bitfield indicating which of the Widget's edges may be dragged to resize the Widget.

## MinSize
`Vector2` · optional

The minimum size to which the Widget may be resized.

## MaxSize
`Vector2` · optional

The maximum size to which the Widget may be resized.

## ForceAspectRatio
`float` · optional

The aspect ratio of the ResizeTarget that will be maintained while resizing. A value of 0 will allow the aspect ratio to be freely changed.

## ResizeLeftCursor
`Cursor` · optional

The Cursor to show when the mouse is over the left edge.

## ResizeRightCursor
`Cursor` · optional

The Cursor to show when the mouse is over the right edge.

## ResizeTopCursor
`Cursor` · optional

The Cursor to show when the mouse is over the top edge.

## ResizeBottomCursor
`Cursor` · optional

The Cursor to show when the mouse is over the bottom edge.

## ResizeTopLeftCursor
`Cursor` · optional

The Cursor to show when the mouse is over the top-left corner.

## ResizeTopRightCursor
`Cursor` · optional

The Cursor to show when the mouse is over the top-right corner.

## ResizeBottomLeftCursor
`Cursor` · optional

The Cursor to show when the mouse is over the bottom-left corner.

## ResizeBottomRightCursor
`Cursor` · optional

The Cursor to show when the mouse is over the bottom-right corner.
