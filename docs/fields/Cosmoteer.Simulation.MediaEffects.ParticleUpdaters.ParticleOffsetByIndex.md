# Cosmoteer.Simulation.MediaEffects.ParticleUpdaters.ParticleOffsetByIndex

Shifts a particle variable by `Offset` times the particle's index, to step a burst out in even increments.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## DataInOut
`ParticleDataID` · optional

The data that will be offset based upon the index.

## IndexIn
`ParticleDataID` · optional

The index that will be used to offset the data.

## Offset
`float` · optional

The offset that will be multiplied with the particle's index.

## Modulus
`int` · optional

The value by which the index will be modulo'ed before being multiplied with Offset. Use 0 to disable.
