# Cosmoteer.Ships.Parts.Resources.ResourceStorageProxyRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## LimitMaxResources
`int` · optional · default `2147483647`

Caps both the resource count and the capacity that the proxy reports for the proxied storage, after [[Cosmoteer.Ships.Parts.Resources.ResourceStorageProxyRules.QuantityScale]] is applied. The vanilla rocket thruster nozzle uses it to limit a chained extender's battery pool to its own local capacity.

## QuantityScale
`float` · optional · default `1`

The amount to scale the proxied storage's quantities by. 1 resource in the proxied storage is worth QuantityScale resources in the proxy. Any resource operations on the proxy are divided by the scale and rounded away from 0.
