# Cosmoteer.Ships.Networks.BasePartNetworkPortRules

Settings shared by the edge ports that join two neighbouring parts into one network, including which port types may connect and how the link is drawn in build mode.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## BlueprintOperationalToggle
`→ PartComponentRules` · optional

The ID of the blueprint component to use for the blueprint port's operational checks, if any.

## BlueprintConnectionMode
`enum BlueprintPortConnectionMode` · optional · one of: `Hypothetical`, `RequireRoutes`, `Always`

The mode used to determine the validity of a connection between two blueprint ports.

## Overlay
`PartNetworkPortOverlayOptions` · optional

Visual options controlling how the port is drawn in the ship's network overlay. Its sub-fields (OverlayIcon, ConnectedEdgeIcon, RouteLine, DrawRouteHub, RouteDrawMode, IconDrawMode, HighlightPartOnPrimaryConnection, PartHighlightColor, SpreadHighlightThroughNetwork) are read directly into the port's own group rather than under a nested `Overlay` key. Purely cosmetic, none of it affects routing or connection validity.

## RouteEndpoint
`→ PartComponentRules` · optional

The IPartNetworkRegistrable component ID that inbound routes should connect to. If null, inbound routes will not be created.

## RouteSource
`→ PartComponentRules` · optional

The IPartNetworkRegistrable component ID that outbound routes should connect from. If null, outbound routes will not be created.

## BlueprintRouteEndpoint
`→ PartComponentRules` · optional

The IPartNetworkRegistrable component ID that inbound blueprint routes should connect to. If null, inbound blueprint routes will not be created.

## BlueprintRouteSource
`→ PartComponentRules` · optional

The IPartNetworkRegistrable component ID that outbound blueprint routes should connect from. If null, outbound blueprint routes will not be created.

## Types
`→ PortType[]` · optional

The port types that the port fulfills, and, if CompatibleTypes is unset, the port types that the port accepts.

## CompatibleTypes
`→ PortType[]` · optional

The port types that the port will accept connections with.

## IncompatibleTypes
`→ PortType[]` · optional

The port types that the port will refuse connections with, even if they are otherwise compatible.
