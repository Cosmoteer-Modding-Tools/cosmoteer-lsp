# Cosmoteer.Simulation.HitEffects.KillCrewEffect

Kills crew standing on the hit part, with `KillChance` rolled per crew member.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## KillChance
`number` · required

The chance to kill a hit crew member.

## AffectEntireParts
`bool` · optional

Whether entire parts are affected on hit, or just the hit cell.

## ImmunityResourcesIsBlacklist
`bool` · optional

Whether ImmunityResources should be treated as a blacklist rather than a whitelist.

## ImmunityResources
`→ ResourceRules[]` · optional

The list of resources that provide immunity from this effect when carried by a crew member. If ImmunityResourcesIsBlacklist is true, this behaviour is inverted.

## ImmuneWhileEmptyHanded
`bool` · optional

Whether a crew member is immune to this effect while not carrying any resources.

## Filter
`EffectFilter` · optional

The filter used to determine hit validity.
