# Halfling.Gui.DropButton

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## FocusController
`WidgetMouseFocusController` · optional

The settings for how and when the user can click to focus this widget.

## FocusStateAudio
`WidgetFocusStateAudio` · optional

The audio sound effects to play when the focus state of this widget changes.

## ArrowRectController
`InnerRectProvider` · optional

The settings for how and where the arrow sprite is shown.

## TargetShownNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when the drop target is shown.

## TargetHiddenNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when the drop target is hidden.

## TargetHiddenDisabledNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when the this DropList is disabled.

## DropTargetController
`WidgetDropTargetController` · optional

The settings for the component that controls the visibility and position of the DropTarget.

## TargetRectController
`WidgetRelativeRectController` · optional

The settings for the component that controls the position of the drop target.
