# Halfling.Gui.DynamicWidget

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## StateNormalNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Normal" state.

## StateHighlightedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Highlighted" state.

## StatePressedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Pressed" or "HotkeyPressed" states.

## StateDisabledNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Disabled" state.

## InputStateController
`WidgetInputStateController` · optional

The settings that control how this widget's input state is determined.

## InputStateAudio
`WidgetInputStateAudio` · optional

The audio sound effects to play when the input state of this widget changes.
