# Cosmoteer.Ships.Parts.Buffs.PartBuffableValueRules

Provides a number that the part's buffs and statuses can modify, which is how a buffed part changes what it does.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Value
`number` · optional

The base value this component reports to components that reference it, defaulting to 1. Being modifiable, it can carry buff and status modifiers, and referencing components see the modified result update live as the part's buffs and statuses change. Vanilla thrusters wrap values like their maximum activation in a `BuffableValue` so overclock buffs can scale what other components read.

## IncludeInBlueprints
`bool` · optional

Creates a blueprint counterpart of the component so build-mode logic can read the value as well. Off by default.
