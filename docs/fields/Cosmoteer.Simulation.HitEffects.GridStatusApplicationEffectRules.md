# Cosmoteer.Simulation.HitEffects.GridStatusApplicationEffectRules

Spreads a status tile by tile across the ship's grid from the hit tile, paying a cost per tile and stopping at walls or at `MaxCost`.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## HitPartSourceRectType
`enum PartRectType` · optional · one of: `Normal`, `Physical`

The type of rect from the hit part to use as the set of source cells. If null, only the hit location cell is used.

## MaxCost
`number` · required

The maximum path cost to a cell for it to be affected.

## HitDirectionCostFactor
`number` · optional

The factor applied to cell cost based on the path direction's alignment with the initial hit direction. For example, a factor of 0.25 with a hit direction of (1,0) would result in 25% of the cost when pathing from cell (5,0) to (6,0), but 100% of the cost pathing from cell (5,0) to (5,1).

## MaxDistance
`number` · optional

The maximum grid-distance to a cell for it to be affected.

## MinDistance
`number` · optional

The minimum grid-distance to a cell for it to be affected.

## UseDistanceForFalloff
`bool` · optional

Whether cell distance instead of cost is used to calculate falloff.

## FalloffPadding
`number` · optional

The value added to the upper limit when calculating the effect of falloff.

## ChanceFalloff
`number` · optional

The exponent for StatusChance falloff over distance.

## ValueFalloff
`number` · optional

The exponent for StatusValue falloff over distance.

## DurationFalloff
`number` · optional

The exponent for StatusDuration falloff over distance.

## StatusValuePool
`range<number>` · optional

The value pool to split across the affected area, weighted according to falloff.

## AllowOverlappingApplications
`bool` · optional · default `true`

Whether the effect is allowed to apply a status to the same part more than once in a single hit. Only applicable to part-layer statuses.

## DivideValueByPartArea
`bool` · optional

Whether the applied value for each tile is divided by the tile area of the part. Only applicable to part-layer statuses.

## DefaultTileCost
`float` · optional · default `1`

The pathing cost of non-empty tiles not otherwise included in PartIDTileCosts, PartCategoryTileCosts.

## EmptyTileCost
`float` · optional · default `1`

The pathing cost of empty tiles.

## PartIDTileCosts
`map<→ PartRules, float>` · optional

The pathing costs of tiles by part ID.

## PartCategoryTileCosts
`map<→ PartCategory, float>` · optional

The pathing costs of tiles by part TypeCategories, not otherwise included in PartIDTileCosts. A part's categories are evaluated in order.

## BlockedByWalls
`bool` · optional

Whether pathing between tiles is blocked by walls.

## AllowDiagonals
`bool` · optional

Whether pathing across diagonals is allowed.
