# Halfling.Gui.ScrollBar

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## DockedChildren
`WidgetDockedChildren`1` · optional

The collection containing the increment and decrement buttons docked to the edges of this widget. The children themselves will NOT be copied.

## Children
`WidgetLayoutChildren`1` · optional

The collection containing the paddle, page up, and page down buttons. The children themselves will NOT be copied.

## IncrementButton
`ImageButton` · optional

The Button that can be used to increment the value of this ScrollBar.

## DecrementButton
`ImageButton` · optional

The Button that can be used to decrement the value of this ScrollBar.

## IncreasePager
`ScrollBarPager` · optional

The ScrollBarPager that can be used to increase the value of this ScrollBar a page at a time.

## DecreasePager
`ScrollBarPager` · optional

The ScrollBarPager that can be used to decrease the value of this ScrollBar a page at a time.

## Paddle
`ScrollBarPaddle` · optional

The ScrollBarPaddle that can be clicked and dragged to adjust the value of this ScrollBar.

## Value
`float` · optional

The current value of this ScrollBar. When setting, the set value will be rounded to a multiple of the Interval property.

## MinValue
`float` · optional

The minimum value of this ScrollBar.

## MaxValue
`float` · optional

The value of this ScrollBar.

## Interval
`float` · optional

The number of which Value will be rounded to a multiple. If 0, no rounding will occur.

## IncrementAmount
`float` · optional

The amount by which the Value will be incremented or decremented when the IncrementButton or DecrementButton is pressed.

## PageSize
`float` · optional

The amount by which the Value will be increased or decreased when the IncreasePager or DecreasePager is pressed.
