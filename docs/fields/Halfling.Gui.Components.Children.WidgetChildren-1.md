# Halfling.Gui.Components.Children.WidgetChildren`1

Contains a list of child widgets that are contained within the parent widget.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ClipPadding
`Borders` · optional

The distances from the widget's borders outside of which children will not be rendered (assuming ClipChildren is true).

## BorderPadding
`Borders` · optional

The distances from the widget's ClipScreenRect that together define the client rectangle in which children are contained. The border padding is cumulative with the clip padding.

## CullChildren
`bool` · optional

Whether all out-of-view children are culled so that they will not be rendered.

## ClipChildren
`bool` · optional

Whether any pixels outside of the ClientScreenRect will be clipped (not rendered).

## ChildrenForceParentRendering
`bool` · optional

Whether any children whose rendering is being forced will also force their parent to render.

## ReverseRenderOrder
`bool` · optional

Whether children will be rendered last-to-first instead of first-to-last.
