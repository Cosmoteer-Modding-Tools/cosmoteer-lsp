# Halfling.Gui.TextEditField/TextFieldWidget

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## StateEnabledTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is not in its "Disabled" state.

## StateDisabledTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is in its "Disabled" state.

## Cursors
`WidgetInputStateCursors` · optional

The settings for the mouse-over and mouse-pressed cursors.

## FocusController
`WidgetMouseFocusController` · optional

The settings for how and when the user can click to focus this widget.

## FocusStateAudio
`WidgetFocusStateAudio` · optional

The audio sound effects to play when the focus state of this widget changes.

## TextSelection
`TextSelectionProvider` · optional

The current state of the text selection.

## MouseTextSelector
`WidgetMouseTextSelector` · optional

The settings for how text can be selected using the mouse.

## KeyboardTextSelector
`WidgetKeyboardTextSelector` · optional

The settings for how text can be selected using the keyboard.

## TextSelectionRenderer
`WidgetTextSelectionRenderer` · optional

The settings for the text selection highlight.

## TextCaretRenderer
`WidgetTextCaretRenderer` · optional

The settings for the text caret.

## TextEditController
`WidgetTextEditController` · optional

The settings for how text can be edited.

## ClipboardController
`WidgetTextClipboardController` · optional

The settings for copying and pasting to and from the operating system's clipboard.

## Text
`string` · optional

The text displayed on this TextFieldWidget.

## MaxTextLength
`int` · optional

The maximum length of the text.
