# Cosmoteer.Generators.Ships.Stages.PlacePartWithStrategyStage

A stage that places some number of a single type of part according to a particular placement strategy.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## PlacementStrategy
`enum PartPlacementStrategy` · optional · one of: `None`, `Centralized`, `Protected`, `AlignWithSimilar`

The strategy to use when picking an optimal location for the part.

## UseWeightedRotationFlips
`bool` · optional

Whether to pick a desired rotation based on weight.

## RotationFlipWeights
`RotationFlipWeightValues` · optional

The weights by which to pick the desired rotation/flip, if UseWeightedRotationFlips is true.
