# Halfling.Gui.Button

A clickable button with text.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## StateNormalTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is in its "Normal" state.

## StateHighlightedTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is in its "Highlighted" state.

## StatePressedTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is in its "Pressed" or "HotkeyPressed" states.

## StateDisabledTextRenderer
`WidgetTextRenderer` · optional

The settings for the text shown when this button is in its "Disabled" state.

## ClickController
`WidgetClickController` · optional

The settings for the component that controls the firing of Clicked events.

## ClickAudio
`WidgetTriggeredAudio` · optional

The audio sound effect played when this button is clicked.

## Text
`string` · optional

The text displayed on this button.

## TextProvider
`ITextProvider` · optional

An ITextProvider object that, if not null, will keep this Button's Text property updated to match the ITextProvider.

## Hotkey
`enum ViKey[]` · optional

The IInputButton (or combination of buttons) which will put the widget into a "HotkeyPressed" state.
