# Halfling.Particles.Updaters.ParticleScalarOperator

Applies `Operator` between a single float variable and a wider one such as a Vector2 or a Color, hitting every component at once.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## AIn
`ParticleDataID` · optional

The data that will be used as the "A" (left-hand) operand.

## DataType
`enum ParticleDataType` · optional · one of: `Float`, `Angle`, `Vector2`, `Vector3`, `Vector4`, `Color`, `Int`, `IntVector2`, `IntVector3`, `IntVector4`, `Vector2D`

The type of data being operated on.

## BIn
`ParticleDataID` · optional

The data that will be used as the "B" (right-hand) operand. This data will not be modified.

## ResultOut
`ParticleDataID` · optional

The data that will be set to the result of the operation.

## Operator
`enum Operator` · optional

The mathematical operator used on DataAInOut and DataBIn.
