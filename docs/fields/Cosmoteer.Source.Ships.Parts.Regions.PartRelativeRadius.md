# Cosmoteer.Source.Ships.Parts.Regions.PartRelativeRadius

An IPartRelativeRegion describing a circular region.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Radius
`float` · required

The radius, in tiles, of the circular region measured from the part's center. A cell counts as inside when its center is within this distance, and the falloff factor runs from 0 at the center to 1 at the radius.

## BlueprintRenderer
`CircleRenderer` · optional

The circle renderer that draws the region outline in the build view, its thickness set to [[Cosmoteer.Source.Ships.Parts.Regions.PartRelativeRadius.Radius]]. Unset means the region draws nothing in the blueprint.
