# Cosmoteer.Simulation.MediaEffects.ParticleUpdaters.ParticleLocationFromPartRect

Places each new particle at a random point inside a part's rectangle, in ship coordinates so the particles ride along with the ship.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## LocationOut
`ParticleDataID` · optional

The data that stores the particle's location.

## RectType
`enum PartRectType` · optional · one of: `Normal`, `Physical`

Which type of part rect to use.

## Inflate
`float` · optional

The amount by which the part's rect is inflated.

## RestrictToPartColliders
`bool` · optional

Whether the particle locations should be restricted to the part's collision geometry.

## SubtractPartCenter
`bool` · optional

Whether the particle locations should be ship or rules relative.

## RotateByDirection
`bool` · optional

Whether the part rect should be rotated by the part direction in ship space.
