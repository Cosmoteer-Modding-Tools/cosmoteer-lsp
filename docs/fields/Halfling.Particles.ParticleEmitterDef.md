# Halfling.Particles.ParticleEmitterDef

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## EmitPerSecond
`float` · optional

The number of particles that are spawned every second.

## EmitPerOneShot
`range<int>` · optional

The number of particles that are spawned every time EmitOneShot() is called.

## PreInitializers
`IParticleDataUpdater[]` · optional

A modifiable list containing the objects used to initialize particle data. These initializers will be run *before* the particle system's own initializers.

## PostInitializers
`IParticleDataUpdater[]` · optional

A modifiable list containing the objects used to initialize particle data. These initializers will be run *after* the particle system's own initializers.
