# Halfling.Particles.Updaters.BaseParticleDataUpdater

Common base for the particle updaters, carrying the editor label plus the `Enabled` and `UpdateAfterInit` switches. A modder writes one of its concrete types.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Name
`string` · optional

The human-readable name of this object.

## Enabled
`bool` · optional

Whether this particle updater is currently enabled.

## UpdateAfterInit
`bool` · optional

Whether this IParticleDataUpdater should be executed immediately after a new particle is initialized.
