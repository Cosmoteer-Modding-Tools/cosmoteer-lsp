# Cosmoteer.Generators.Textures.HeightMapLayer

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## OpacityInRange
`range<float>` · optional

A range of values of the existing heightmap data in which this layer's opacity will ramp from 0 to 1.

## OpacityOutRange
`range<float>` · optional

A range of values of the existing heightmap data in which this layer's opacity will ramp from 1 to 0.

## Name
`string` · optional

The name of this layer. Only used in the generator editor interface.

## Enabled
`bool` · optional

Whether this layer is enabled.

## Opacity
`float` · optional

The opacity of this layer.

## BlendMode
`enum HeightMapLayerBlendMode` · optional · one of: `Add`, `Subtract`, `Multiply`, `Replace`, `Min`, `Max`

Specifies how this layer will be applied to the previous layer's results.

## HeightScale
`float` · optional

The factor applied to the values of this layer.
