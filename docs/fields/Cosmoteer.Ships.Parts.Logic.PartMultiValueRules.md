# Cosmoteer.Ships.Parts.Logic.PartMultiValueRules

Combines several value components into one number by adding, multiplying or taking the smallest, largest or average.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## IncludeInBlueprints
`enum BlueprintInclusionMode` · optional · one of: `None`, `Normal`, `All`

Whether a blueprint version of this component is also created for ship blueprints. `None` creates none, `Normal` includes it in regular blueprint updates, and `All` additionally includes it in lightweight blueprint validity checks. `false` and `true` are accepted as aliases for `None` and `Normal`.
