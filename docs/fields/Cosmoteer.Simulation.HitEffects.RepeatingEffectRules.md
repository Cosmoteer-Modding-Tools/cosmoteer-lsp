# Cosmoteer.Simulation.HitEffects.RepeatingEffectRules

Fires the same hit effects again every `Interval` seconds for `Count` repetitions, for burning and other lingering damage.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## HitEffects
`MultiHitEffectRules` · optional

The hit effects to perform every repetition.

## MediaEffects
`MultiMediaEffectRules` · optional

The media effects to play every repetition.

## Interval
`number` · required

The period between one repetition and the next.

## Count
`number` · required

The number of total repetition.

## InitialDelay
`number` · optional

The delay before starting the first repetition.

## RequireHitPart
`bool` · optional

Whether to end the repeating effect if the hit part is null or destroyed.

## HitEffectsScaleOverIterations
`range<number>` · optional

The multiplier of effectScale over the duration of the repeat. Minimum on the first iteration to maximum on the last.

## HitEffectsScaleOverIterationsExponent
`float` · optional · default `1`

The exponent to which the normalised iteration progress value is raised, which is used to sample HitEffectsScaleOverIterations.
