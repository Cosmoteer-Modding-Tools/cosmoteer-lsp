# Cosmoteer.Bullets.BulletRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ID
`→ BulletRules` · optional

Identifier that must be unique across the game.

## OtherIDs
`→ BulletRules[]` · optional

Legacy identifiers. You shouldn't normally need these.

## Range
`number` · required

How far the target can be when the projectile is shot at it.

## IdealRange
`range<number>` · optional

The range from which the AI will try to use this weapon.

## IdealRadius
`range<number>` · optional

<!-- TODO: needs documentation -->

## Speed
`range<number>` · optional

How fast the projectile moves forward when spawned.

## ReceivableBuffs
`→ BuffType[]` · optional

<!-- TODO: needs documentation -->

## ForgetTarget
`bool` · optional

<!-- TODO: needs documentation -->

## RandomRotation
`bool` · optional

Whether the projectile spawns facing a random direction.
