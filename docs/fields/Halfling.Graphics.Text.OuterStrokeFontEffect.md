# Halfling.Graphics.Text.OuterStrokeFontEffect

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Color
`IntColor` · required

The color that will be applied to the font's edges.

## RelativeThickness
`float` · optional

The thickness of the stroke effect relative to the size of the font. This will be added with AbsoluteThickness.

## AbsoluteThickness
`float` · optional

The thickness of the stroke effect in pixels. This will be added with RelativeThickness.

## Falloff
`float` · optional

A number that indicates how quickly the stroke effect will disappear as the distance from the edge of the font increases.
