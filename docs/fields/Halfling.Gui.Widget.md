# Halfling.Gui.Widget

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Name
`string` · optional

The name of this Widget. This property has no effect within the Halfling GUI library, but may be useful to the user program or for debugging purposes.

## AnchorRect
`Rect` · optional

The rect in the parent to which this Widget is anchored. Use the AnchorPresets static class for a list of pre-defined anchor rects.

## Rect
`Rect` · optional

The rectangular position of this Widget relative to AnchorRect in pixel coordinates.

## Location
`Vector2` · optional

The location of the upper-left of this Widget relative to the upper-left corner of AnchorRect in pixel coordinates.

## Size
`Vector2` · optional

The size of this Widget assuming that the size of AnchorRect is zero.

## X
`float` · optional

The x-coordinate of the left edge of this Widget relative to the left edge of AnchorRect in pixel coordinates.

## Y
`float` · optional

The y-coordinate of the top of this Widget relative to the top edge of AnchorRect in pixel coordinates.

## Width
`float` · optional

The width of this Widget in pixel coordinates assuming that the widget of AnchorRect is zero.

## Height
`float` · optional

The height of this Widget in pixel coordinates assuming that the height of AnchorRect is zero.

## Left
`float` · optional

The left edge of this widget relative to the left edge of AnchorRect in pixel coordinates. The position of the right edge will remain unmodified.

## Right
`float` · optional

The right edge of this widget relative to the right edge of AnchorRect in pixel coordinates. The position of the left edge will remain unmodified.

## Top
`float` · optional

The top edge of this widget relative to the top edge of AnchorRect in pixel coordinates. The position of the bottom edge will remain unmodified.

## Bottom
`float` · optional

The bottom edge of this widget relative to the bottom edge of AnchorRect in pixel coordinates. The position of the top edge will remain unmodified.

## PercentileRect
`Rect` · optional

Sets the rectangular position of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileLocation
`Vector2` · optional

Sets the upper-left corner of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileSize
`Vector2` · optional

Sets the size of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileX
`float` · optional

Sets the x-coordinate of the left edge of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileY
`float` · optional

Sets the y-coordinate of the top edge of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileWidth
`float` · optional

Sets the width of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileHeight
`float` · optional

Sets the height of this Widget as a 0-100 percentage of its parent. Modifies the Rect and AnchorRect properties as needed.

## PercentileLeft
`float` · optional

Sets the left edge of this Widget as a 0-100 percentage of its parent without modifying the right edge. Modifies the Rect and AnchorRect properties as needed.

## PercentileRight
`float` · optional

Sets the left edge of this Widget as a 0-100 percentage of its parent without modifying the left edge. Modifies the Rect and AnchorRect properties as needed.

## PercentileTop
`float` · optional

Sets the top edge of this Widget as a 0-100 percentage of its parent without modifying the bottom edge. Modifies the Rect and AnchorRect properties as needed.

## PercentileBottom
`float` · optional

Sets the bottom edge of this Widget as a 0-100 percentage of its parent without modifying the top edge. Modifies the Rect and AnchorRect properties as needed.

## Active
`bool` · optional

Whether this particular Widget is active. This Widget's IsActive property will only be true if its SelfActive property is true, it has a parent, and its parent's IsActive property is true.

## SelfInputActive
`bool` · optional

Whether this particular Widget is itself set to respond to user input. This Widget's IsInputActive property will only be true its SelfInputActive property is true, it has a parent, and its parent's IsInputActive property is true.

## DockMode
`enum DockMode` · optional · one of: `Left`, `Right`, `Top`, `Bottom`

Which edge of its parent this widget will be docked to when placed in a WidgetDockedChildren list.

## IgnoreInLayout
`bool` · optional

Whether this widget will be ignored during automatic widget layout.

## InputBlockMode
`enum InputBlockMode` · optional · one of: `Auto`, `Always`, `Never`

Whether this widget blocks user input to widgets underneath it.

## RenderSettings
`WidgetRenderSettings` · optional

The settings that control how this Widget will be rendered. Settings specified here will be inherited by all child components and widgets.

## Animator
`WidgetAnimator` · optional

The object that controls how this widget is animated.

## Triggers
`WidgetTriggerManager` · optional

The collection of triggers attached to this widget.

## ToolTips
`WidgetToolTips` · optional

The settings for displaying mouse-over tool tips for this widget.

## ToolTipDisplay
`IToolTipDisplay` · optional

The object that displays tool tips for this widget and any child widgets that don't themselves have a ToolTipDisplay set.

## AutoSize
`WidgetAutoSizeController` · optional

The settings for how this widget is automatically sized.

## CustomShapeFunc
`CustomShapeFunc` · optional

The function used to determine whether a mouse cursor is "inside of" this Widget. A null value will simply check against this Widget's ScreenRect.

## UserData
`Object` · optional

An arbitrary data object attached to this Widget. The user application can attach anything it likes to this property.
