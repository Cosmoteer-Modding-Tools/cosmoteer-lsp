# Cosmoteer.Game.Gui.ToggledImageButton

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## StateNormalSelectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Normal" state and is selected.

## StateHighlightedSelectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Highlighted" state and is selected.

## StatePressedSelectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Pressed" or "HotkeyPressed" states and is selected.

## StateDisabledSelectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice shown when this widget is in its "Disabled" state and is selected.

## ImageRectController
`InnerRectProvider` · optional

The settings for how and where the image is shown.

## ImageSprite
`WidgetSingleSprite` · optional

The settings for the image shown on this button.
