# Halfling.Gui.Components.Rects.InnerRectProvider

Provides a rectangle that is located inside another provided rect.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## InnerRectSize
`Vector2` · optional

The size of the provided inner rect.

## InnerRectWidth
`float` · optional

The width of the provided inner rect.

## InnerRectHeight
`float` · optional

The height of the provided inner rect.

## InnerRectMode
`enum InnerRectMode` · optional

How and where the inner rect will be located inside the provided rect.

## BorderPadding
`Borders` · optional

The amount of padding to apply to the borders of the outer rect.

## RemainderPadding
`Borders` · optional

The amount of padding to apply to the borders of the remainder rect.

## ClipInnerRect
`bool` · optional

Whether the provided inner rect will be clipped so that it doesn't overhang the outer rect.
