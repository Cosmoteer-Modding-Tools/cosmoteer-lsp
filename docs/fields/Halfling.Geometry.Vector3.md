# Halfling.Geometry.Vector3

Stores a 3-dimensional point or displacement in space.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## X
`float` · required

The X (1st) component of this Vector3.

## Y
`float` · required

The Y (2nd) component of this Vector3.

## Z
`float` · required

The Z (3rd) component of this Vector3.

## 0
`float` · optional

The X (1st) component when the vector is written in list form `[x, y, z]`.

## 1
`float` · optional

The Y (2nd) component when the vector is written in list form `[x, y, z]`.

## 2
`float` · optional

The Z (3rd) component when the vector is written in list form `[x, y, z]`.
