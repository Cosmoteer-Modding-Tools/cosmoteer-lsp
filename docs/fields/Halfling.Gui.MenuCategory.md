# Halfling.Gui.MenuCategory

A list of menu items that is accessed by clicking on a header item.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## IconRectController
`InnerRectProvider` · optional

The settings for how and where the icon sprite is shown.

## IconSprite
`WidgetSingleSprite` · optional

The settings for the icon shown on this menu item.

## IconBackingSprite
`WidgetSingleSprite` · optional

The settings for the background shown behind the icon on this menu item.

## FocusController
`WidgetMenuFocusController` · optional

The settings for how and when the user can click to focus this widget.

## FocusStateAudio
`WidgetFocusStateAudio` · optional

The audio sound effects to play when the focus state of this widget changes.

## DropTargetController
`WidgetDropTargetController` · optional

The settings for the component that controls the visibility and position of the MenuBox.

## MenuBox
`ScrollBox` · optional

The ScrollBox widget that is shown underneath the button when the button is clicked.

## MenuBoxRectController
`WidgetRelativeRectController` · optional

The settings for the component that controls the position of the MenuBox widget.

## FocusedWidgetManager
`FocusedWidgetManager` · optional

The FocusedWidgetManager that manages the focus of all the widgets in ScrollBox.
