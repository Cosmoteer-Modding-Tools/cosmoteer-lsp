# Halfling.Particles.Updaters.ParticleValueCurve

Drives a particle variable along a curve of `Points` as `LerpIn` runs from 0 to 1, for fades that are not a straight line.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## MinValue
`float` · optional

The minimum output value.

## MaxValue
`float` · optional

The maximum output value.

## LerpIn
`ParticleDataID` · optional

The data that stores the T values used for the lerp.

## CurveOut
`ParticleDataID` · optional

The data that will received the lerped colors.

## Invert
`bool` · optional

Whether the LerpIn value should be inverted.

## Points
`float[]` · optional

The array of points from which the output value will be interpolated.
