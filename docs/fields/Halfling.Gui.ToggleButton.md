# Halfling.Gui.ToggleButton

A SelectableButton that display a check sprite depending on whether it is selected.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ToggleRectController
`InnerRectProvider` · optional

The settings for how and where the toggle sprite is shown.

## SelectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when this widget is selected.

## DeselectedNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when this widget is deselected.

## SelectedDisabledNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when this widget is selected and its input is disabled.

## DeselectedDisabledNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice sprite shown when this widget is deselected and its input is disabled.
