# Halfling.Gui.DropList`1

A list box that is accessed by clicking on a button. The list box is displayed underneath the button as a "drop down".

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ListBox
`ListBox`1` · optional

The ListBox widget that is shown underneath the button when the button is clicked.

## MatchTextToSelection
`bool` · optional

Whether the text of the Button will be automatically set to match the text of the selected item.

## HideListWhenSelected
`bool` · optional

Whether the ListBox will be hidden when one of its items is selected.

## AutoCenterOnSelected
`bool` · optional

Whether the ListBox will be automatically centered on the selected item when it is opened.
