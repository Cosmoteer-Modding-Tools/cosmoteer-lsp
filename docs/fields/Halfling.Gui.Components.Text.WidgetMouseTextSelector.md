# Halfling.Gui.Components.Text.WidgetMouseTextSelector

A widget component that allows the user to position a text caret or select a range of text using the mouse.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## MouseButton
`enum ViKey[]` · optional

The mouse IInputButton that can be used to select text.

## AppendSelectionKey
`enum ViKey[]` · optional

The IInputButton that can be held to append text to the selection instead of replacing the selection.

## DoubleClickWordSelectMaxInterval
`Time` · optional

The maximum amount of time between clicks that will result in a whole word being selected.

## DisableDuringImeTextComposition
`bool` · optional

Whether text selection will be disabled while the OS input method editor is composing text.
