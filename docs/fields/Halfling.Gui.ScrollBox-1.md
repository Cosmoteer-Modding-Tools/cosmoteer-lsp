# Halfling.Gui.ScrollBox`1

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## HorizontalScrollBar
`ScrollBar` · optional

The ScrollBar that can be used to pan the contents of this ScrollBox horizontally.

## VerticalScrollBar
`ScrollBar` · optional

The ScrollBar that can be used to pan the contents of this ScrollBox vertically.

## CornerWidget
`StaticWidget` · optional

The widget that sits in the corner when both ScrollBars are active.

## ScrollBarController
`WidgetScrollBarController` · optional

The settings for the component that controls the ScrollBars.

## ScrollWheelController
`WidgetScrollWheelController` · optional

The settings for the component that controls how the user can use the mouse scroll wheel to scroll the view.
