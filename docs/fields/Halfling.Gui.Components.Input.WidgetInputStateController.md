# Halfling.Gui.Components.Input.WidgetInputStateController

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## BlocksInput
`bool` · optional

Whether user input to widgets underneath will be blocked.

## MouseButton
`enum ViKey[]` · optional

The mouse IInputButton to which the widget will respond.

## Hotkey
`enum ViKey[]` · optional

The IInputButton (or combination of buttons) which will put the widget into a "HotkeyPressed" state.

## OverrideInputActive
`bool` · optional

If not null, overrides the attached widget's IsInputActive state for the purposes of this WidgetInputStateController.

## AllowHighlighted
`bool` · optional

Whether the widget is allowed to be in the "Highlighted" input state.

## AllowPressed
`bool` · optional

Whether the widget is allowed to be in the "Pressed" input state.

## AllowHotkeyPressed
`bool` · optional

Whether the widget is allowed to be in the "HotkeyPressed" input state.
