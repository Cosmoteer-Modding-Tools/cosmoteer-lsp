# Halfling.Gui.Components.ScrollBars.PaddleRectController

Given an IRectProvider and an IRangedValueProvider, provides a rect indicating where the value is within its range.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## PaddleDirection
`enum PaddleDirection` · optional · one of: `LeftToRight`, `RightToLeft`, `TopToBottom`, `BottomToTop`

The direction in which a paddle will move as the value increases.

## PaddleSizeMode
`enum PaddleSizeMode` · optional · one of: `FixedPixel`, `DynamicValue`

The method that will be used to determine the size of the paddle.

## PaddlePixelSize
`float` · optional

The size (width or height) of the paddle in pixels when the PaddleSizeMode is FixedPixel.

## PaddleValueSize
`float` · optional

The size (width or height) of the paddle in value-relative units when the PaddleSizeMode is DynamicValue.

## MinPaddlePixelSize
`float` · optional

The minimum size (width or height) of the paddle when the PaddleSizeMode is DynamicValue.
