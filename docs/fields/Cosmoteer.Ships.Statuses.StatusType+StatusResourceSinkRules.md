# Cosmoteer.Ships.Statuses.StatusType/StatusResourceSinkRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Resource
`→ ResourceRules` · required

The requested resource type of the sink.

## AssociateJobWithPart
`→ PartRules` · optional

The PartRules to associate with the sink's delivery jobs, if any.

## JobNameKey
`string` · optional

The key for the delivery job's priority name.

## PrioritySortOrder
`int` · optional · default `-1`

The sort order for the delivery job's priority.

## DefaultPriority
`int` · required

The default priority for delivery jobs to this sink.

## ValueChangePerResource
`float` · optional · default `1`

The change in status value per resource delivered.

## TargetValue
`float` · required

The value that resources will be delivered in order to reach.

## AlwaysRequestMaxResources
`bool` · optional

Whether the status sinks will request the maximum amount of resources that crew can carry, regardless of remaining capacity.

## DeliveryMediaEffects
`MultiMediaEffectRules` · optional

The continuous media effects to play while delivering resources to the sink.

## DeliveryHitEffects
`MultiHitEffectRules` · optional

The hit effects to perform on delivery of resources. EffectScale is set to the resource quantity.

## ReassignmentSearchIterations
`int` · optional

The number of search iterations to perform when reassigning crew from removed sinks. Behaviour is disabled at 0.
