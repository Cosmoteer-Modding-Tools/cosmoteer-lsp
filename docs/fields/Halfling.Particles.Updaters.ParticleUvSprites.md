# Halfling.Particles.Updaters.ParticleUvSprites

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## SpriteIndexIn
`ParticleDataID` · optional

The data that stores the particle's sprite index.

## UvRectOut
`ParticleDataID` · optional

The data that stores the particle's UV rect coordinates.

## TextureSize
`IntVector2` · optional

The size of the texture containing the sprites.

## SpriteSize
`IntVector2` · optional

The size of an individual sprite in the texture.

## SpriteCount
`int` · optional

The number of sprites in the animation.

## SpritesPerRow
`int` · optional

The number of sprites in a single row in the texture.

## PixelOffset
`IntVector2` · optional

The offset in pixels of the sprites in the texture.

## Looping
`bool` · optional

Whether the index should loop after reaching the end. If false, the index will be clamped by the sprite count.
