# Halfling.Gui.Components.Focus.WidgetMouseFocusController

Extends the WidgetFocusController class such that the mouse can be used in various ways to focus and defocus the Widget.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## EnableClickInsideToFocus
`bool` · optional

Whether clicking the inside of the Widget will cause it to be focused.

## EnableClickInsideToDefocus
`bool` · optional

Whether clicking inside of the Widget will cause its focus to be relinquished.

## EnableClickOutsideToDefocus
`bool` · optional

Whether clicking outside of the Widget will cause its focus to be relinquished.

## EnableMouseEnterToFocus
`bool` · optional

Whether moving the mouse inside the Widget will cause it to be focused.

## EnableMouseLeaveToDefocus
`bool` · optional

Whether moving the mouse outside of the Widget will cause its focus to be relinquished.

## EnableChainedWidgetChecking
`bool` · optional

When EnableClickOutsideToDefocus is true or EnableMouseLeaveToDefocus is true, gets or sets whether clicking on or mousing-over another Widget chained to the attached Widget will prevent the attached Widget from defocusing.

## MouseButton
`enum ViKey[]` · optional

The IInputButton that can be used to focus and defocus the Widget.
