# Halfling.Gui.ProgressBar

A static Widget with an overylay sprite that is clipped according to a value.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ProgressNineSlice
`WidgetNineSlice` · optional

The settings for the nine-slice that is rendered according to the value of this ProgressBar.

## Fill
`FilledRectProvider` · optional

The settings for how the ProgressSprite is filled as the Value property increases.

## ProgressSpriteUVs
`WidgetSpriteUVManager` · optional

The settings for whether and how the ProgressSprite's UVRect will be adjusted depending on the value of this Progressbar.

## Value
`float` · optional

The current value to display on this ProgressBar.

## EmptyValue
`float` · optional

The value at which this ProgressBar will be completely empty.

## FullValue
`float` · optional

The value at which this ProgressBar will be completely full.
