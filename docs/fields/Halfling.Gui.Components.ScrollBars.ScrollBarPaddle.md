# Halfling.Gui.Components.ScrollBars.ScrollBarPaddle

A draggable Button that functions as the paddle of a ScrollBar.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## RectController
`PaddleRectController` · optional

The settings for the object that controls this ScrollBarPaddle's ScreenRect.

## DragController
`PaddleDragController` · optional

The settings for the object that controls how this ScrollBarPaddle is dragged.

## ShowValueText
`bool` · optional

Whether text displaying the ScrollBar's current value will be rendered on this paddle.

## ValueTextFormat
`string` · optional

The format string to use when ShowValueText is true. If null, the text will not be formatted.
