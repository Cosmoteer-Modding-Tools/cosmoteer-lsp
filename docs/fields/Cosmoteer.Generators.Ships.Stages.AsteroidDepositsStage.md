# Cosmoteer.Generators.Ships.Stages.AsteroidDepositsStage

Fills deposits of parts within an asteroid.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Parts
`→ PartRules[]` · optional

The type of parts to place.

## ReplaceableParts
`→ PartRules[]` · optional

The list of part types that may be replaced by this stage.

## MinPartsFraction
`float` · optional · default `0.5`

The minimum number of parts to add.

## MaxPartsFraction
`float` · optional · default `0.5`

The maximum number of parts to add.

## MinPartsPerDeposit
`int` · optional · default `10`

The minimum number of parts to add per deposit area.

## MaxPartsPerDeposit
`int` · optional · default `10`

The maximum number of parts to add per deposit area.
