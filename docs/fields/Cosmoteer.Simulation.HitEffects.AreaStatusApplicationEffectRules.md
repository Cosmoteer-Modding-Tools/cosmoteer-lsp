# Cosmoteer.Simulation.HitEffects.AreaStatusApplicationEffectRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Radius
`number` · required

The radius within which targets will be affected.

## ExclusionRadius
`number` · optional

The inner radius within which cells will be ignored.

## ChanceFalloff
`number` · optional

<!-- TODO: needs documentation -->

## ValueFalloff
`number` · optional

<!-- TODO: needs documentation -->

## DurationFalloff
`number` · optional

<!-- TODO: needs documentation -->

## AllowOverlappingApplications
`bool` · optional · default `true`

Whether the effect is allowed to apply a status to the same part more than once in a single hit. Only applicable to part-layer statuses.

## DivideValueByPartArea
`bool` · optional

Whether the applied value for each tile is divided by the tile area of the part. Only applicable to part-layer statuses.

## AffectHitShipOnly
`bool` · optional

Whether only the hit ship should be affected.
