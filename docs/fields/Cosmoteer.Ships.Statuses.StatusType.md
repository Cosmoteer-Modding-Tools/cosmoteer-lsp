# Cosmoteer.Ships.Statuses.StatusType

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ID
`→ StatusType` · required

The identifier other rules use to refer to this status, for example the `StatusType` field of a StatusApplication hit effect, a StatusToggle component or a StatusValueRegulator. Vanilla ids are namespaced like `cosmoteer.fire`.

## IndicatorSprite
`AtlasSprite` · optional

The sprite displayed over instances of this status.

## Layer
`enum StatusLayer` · required · one of: `Tile`, `Part`

The layer that instances of this status are applied to.

## StatusCombineMode
`enum StatusCombineMode` · optional · one of: `ApplyNewInstance`, `AbortApplication`, `CombineValue`, `Replace`, `Extend`, `ExtendAndCombineValue`, `Refresh`, `RefreshAndCombineValue`

How applications of this status are handled when another status of this type exists in the same location.

## ValueCombiner
`ValueCombinerRules` · optional

Provides the final value when combining multiple status instances.

## ValueClampRange
`range<float>` · optional

The range that instances of this StatusType will have their values clamped to.

## RemoveAtMinValue
`bool` · optional

Whether an instance is deleted as soon as its value reaches `ValueClampRange` Min. It also stops diffusion and status application from creating an instance that would start at exactly that minimum. See `TriggerEffectsOnMinValueRemove` for whether `RemovalEffects` fire in that case.

## TriggerEffectsOnMinValueRemove
`bool` · optional · default `true`

Whether `RemovalEffects` and `RemovalMediaEffects` are performed when an instance is deleted by `RemoveAtMinValue`. Has no bearing on removals from any other cause.

## TransferToUnderlyingOnPartDestroyed
`bool` · optional

Whether tile-based statuses will transfer to underlying parts when the part they're on is destroyed.

## NegativeCombineIgnoresResistance
`bool` · optional

Whether combinatory status applications with negative values will ignore value resistances.

## CrewSpeedFactor
`range<float>` · optional

The range value to multiply crew speed by, sampled by interpolated status value.

## JobInterruptionSeverity
`int` · optional

The severity value used to determine whether certain jobs should be cancelled by this status, if any.

## Filter
`StatusApplicationFilter` · optional

Restricts which parts and cells this status can be applied to. The same filter also decides whether a status may transfer onto a replacement part under `TransferToUnderlyingOnPartDestroyed`, and whether diffusion may seed a new instance in a cell.

## ResourceSink
`StatusResourceSinkRules` · optional

The rules defining the status' resource sink behaviour, if any.

## SoftRepair
`StatusSoftRepairRules` · optional

The rules defining the status' soft-repair behaviour, if any.

## Diffusion
`DiffusionRules` · optional

Makes the status spread between neighbouring cells each physics tick, the way heat conducts through a ship. Only works when `Layer` is `Tile` and `StatusCombineMode` is not `ApplyNewInstance`.

## ValueModulators
`MultiValueModulatorRules` · optional

The value modulators applied to the values of this status' instances.

## ValueModulationResistanceRange
`range<float>` · optional

The range that status resistance values are clamped to for ValueModulators.

## TickInterval
`Time` · optional

The interval between each call of this status' TickingEffects and TickingMediaEffects.

## RequireValueRangeForTick
`range<float>` · optional

The status value range that ticking effects will be performed for. Statuses with values outside this range will be ignored.

## ClearPlayerSourcesOnAggressionLoss
`bool` · optional · default `true`

Whether the instances of this status should have their source player cleared on loss off aggression (e.g. truces).

## ClearOnShipJunked
`bool` · optional · default `true`

Whether the instances of this status should be removed (without triggering effects) on the ship being junked.

## MediaIntensityValueType
`enum StatusValueType` · optional · one of: `Raw`, `Interpolated`

Whether the status value handed to media effects as their Intensity is the raw value or interpolated into a 0 to 1 fraction. Interpolation uses `MediaIntensityInterpolationRange` when set, otherwise `ValueClampRange`.

## UseDurationForMediaIntensity
`bool` · optional

Whether the media effect Intensity is derived from the status' remaining duration in seconds rather than from its value. Vanilla fire foam uses this so the foam effect fades out as the foam expires.

## MediaIntensityInterpolationRange
`range<float>` · optional

The value range mapped onto 0 to 1 when `MediaIntensityValueType` is `Interpolated`. Leave unset to reuse `ValueClampRange`, which is what you want unless the visual should saturate well before the gameplay maximum.

## ClampMediaIntensity
`bool` · optional · default `true`

Whether the interpolated media Intensity is clamped to 0 to 1. Only has an effect when `MediaIntensityInterpolationRange` is set, since the fallback to `ValueClampRange` always clamps. Vanilla heat turns it off so the glow keeps intensifying past 1000 heat.

## ParentMediaEffectsToShip
`bool` · optional

Whether media effect nodes are attached to the ship and positioned in ship-local space, so they move and rotate with it, instead of being spawned at a fixed world location.

## StatusResistances
`map<→ StatusType, StatusResistanceRules>` · optional

The status resistances provided to underlying parts by instances of this status type.

## DamageResistances
`map<→ DamageType, SimpleResistanceRules>` · optional

The damage resistances provided to underlying parts by instances of this status type.

## PenetrationResistanceModifier
`SimpleResistanceRules` · optional

The modifier applied to the penetration resistances of parts underlying instances of this status type.

## ApplicationEffects
`MultiHitEffectRules` · optional

The hit effects performed when this status is applied.

## ApplicationMediaEffects
`MultiMediaEffectRules` · optional

The media effects played when this status is applied.

## TickingEffects
`MultiHitEffectRules` · optional

The hit effects performed on each tick of this status.

## TickingMediaEffects
`MultiMediaEffectRules` · optional

The media effects played on each tick of this status.

## ContinuousMediaEffects
`MultiMediaEffectRules` · optional

The media effects played continuously on application of this status, and stopped on status removal.

## CustomShapeEffects
`StatusCustomShapeEffectRules` · optional

The media effects played continuously using a single effect node with an ICustomEffectShape. Supports only a subset of media effects.

## RemovalEffects
`MultiHitEffectRules` · optional

The hit effects performed on removal of this status.

## RemovalMediaEffects
`MultiMediaEffectRules` · optional

The media effects played on removal of this status.
