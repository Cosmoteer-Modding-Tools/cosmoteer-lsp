# Halfling.Gui.Components.Graphics.WidgetNineSlice

A widget component that renders a nine-sliced sprite to the screen.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## TopLeftSprite
`GuiSprite` · optional

The top-left corner sprite.

## TopSprite
`GuiSprite` · optional

The top edge sprite.

## TopRightSprite
`GuiSprite` · optional

The top-right corner sprite.

## LeftSprite
`GuiSprite` · optional

The left edge sprite.

## CenterSprite
`GuiSprite` · optional

The center sprite.

## RightSprite
`GuiSprite` · optional

The right edge sprite.

## BottomLeftSprite
`GuiSprite` · optional

The bottom-left corner sprite.

## BottomSprite
`GuiSprite` · optional

The bottom edge sprite.

## BottomRightSprite
`GuiSprite` · optional

The bottom-right corner sprite.

## Borders
`Borders` · optional

The sizes of each of the border tiles of this nine-slice.

## Bleed
`Borders` · optional

The amount which this nine-slice "bleeds" past the edges of the provided rect.

## ProvidedRectPadding
`Borders` · optional

The amount of padding to add to the rectangle provided by this WidgetNineSlice.

## Flags
`enum NineSliceFlags` · optional · one of: `None`, `Left`, `Top`, `Right`, `Bottom`, `Center`, `Borders`, `All`

Bitwise-combinable flags that specify which sprites of this nine-slice are rendered.
