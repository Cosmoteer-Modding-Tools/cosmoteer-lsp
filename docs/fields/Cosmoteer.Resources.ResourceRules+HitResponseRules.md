# Cosmoteer.Resources.ResourceRules/HitResponseRules

Makes stored resources react when their part is hit, playing effects and optionally being lost, at a chance that scales with the damage dealt.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## HitEffects
`MultiHitEffectRules` · optional

The hit effects to perform, if any.

## MediaEffects
`MultiMediaEffectRules` · optional

The media effects to play, if any.

## Chance
`float` · optional

The default base chance to respond to a hit.

## ChancePerNDamage
`float` · optional

The chance added per NDamage in a hit.

## NDamage
`float` · optional · default `1`

The quantity of damage in a hit that adds a response chance of ChancePerNDamage.

## DamageTypeOverrides
`map<→ DamageType, DamageTypeOverrideData>` · optional

Per-damage-type response overrides.

## RequireDirectHit
`bool` · optional

Whether triggering a response requires a specified HitArea that includes the resource's location.

## RemoveOnResponse
`bool` · optional · default `true`

Whether the responding resources should be removed on a successful response.
