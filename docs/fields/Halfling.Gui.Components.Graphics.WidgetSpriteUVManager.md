# Halfling.Gui.Components.Graphics.WidgetSpriteUVManager

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Enabled
`bool` · optional

Whether this WidgetSpriteUVManager will adjust the given WidgetSingleSprite's UVRect. Set this to false to not modify its UVRect.

## BaseUVRect
`Rect` · optional

The UVRect that will be used when the entire sprite should be displayed.

## UMode
`enum SpriteUVMode` · optional · one of: `Stretch`, `Crop`, `Repeat`

How the Sprite's UVRect's X and Width will be calculated.

## VMode
`enum SpriteUVMode` · optional · one of: `Stretch`, `Crop`, `Repeat`

How the Sprite's UVRect's Y and Height will be calculated.

## RepeatWidth
`float` · optional

Distance over which the Sprite's U coordinates will repeat when UMode is Repeat.

## RepeatHeight
`float` · optional

The distance over which the Sprite's V coordinates will repeat when VMode is Repeat.
