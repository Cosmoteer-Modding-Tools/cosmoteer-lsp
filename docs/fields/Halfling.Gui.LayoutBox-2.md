# Halfling.Gui.LayoutBox`2

A widget that contains some number of child widgets and posesses the capability of automatically arranging those widgets according to a desired mode.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## DockedChildren
`WidgetDockedChildren`1` · optional

The children docked to the edges of this widget. The remaining area will be used by the widgets in the Children collection. The children themselves will NOT be copied.

## Children
`WidgetLayoutChildren`1` · optional

The children of this widget and options for controlling their automatic layout. The children themselves will NOT be copied.

## ChildMask
`NineSlicedMask` · optional

The component that masks the rendering of child components.
