# Halfling.Gui.MenuItem

A kind of Button that is intended to be displayed within a menu category.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## CloseWhenClicked
`bool` · optional

Whether the menu hierarchy will be closed when this MenuItem is clicked.

## IconRectController
`InnerRectProvider` · optional

The settings for how and where the icon sprite is shown.

## IconSprite
`WidgetSingleSprite` · optional

The settings for the icon shown on this menu item.

## IconBackingSprite
`WidgetSingleSprite` · optional

The settings for the background shown behind the icon on this menu item.
