# Halfling.Gui.Components.ToolTips.WidgetToolTips

Contains settings and logic for displaying tool tips on mouse over.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Text
`string` · optional

The text displayed on mouse-over when the widget's input is enabled.

## TextProvider
`ITextProvider` · optional

An ITextProvider object that, if not null, will keep this WidgetToolTip's Text property updated to match the ITextProvider.

## InputDisabledText
`string` · optional

The text displayed on mouse-over when the widget's input is disabled.

## InputDisabledTextProvider
`ITextProvider` · optional

An ITextProvider object that, if not null, will keep this WidgetToolTip's InputDisabledText property updated to match the ITextProvider.
