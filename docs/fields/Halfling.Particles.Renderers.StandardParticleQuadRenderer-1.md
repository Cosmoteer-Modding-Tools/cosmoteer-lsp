# Halfling.Particles.Renderers.StandardParticleQuadRenderer`1

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## LocationIn
`ParticleDataID` · optional

The data that stores the particle locations.

## ScaleIn
`ParticleDataID` · optional

The data, if any, that stores the particle scale factors.

## Scale2In
`ParticleDataID` · optional

The data, if any, that stores the particle scale factors.

## RotationIn
`ParticleDataID` · optional

The data, if any, that stores the particle rotations.

## ColorIn
`ParticleDataID` · optional

The data, if any, that stores the particle colors.

## UvRectIn
`ParticleDataID` · optional

The data, if any, that stores the UV rects.

## BaseSize
`Vector2` · optional

The base size of the particle quads.

## Offset
`Vector2` · optional

The how much the quad is offset relative to its size. The quad will scale and rotate around the quad's *original* center, not its post-offset center.
