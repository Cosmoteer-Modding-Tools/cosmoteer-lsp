# Cosmoteer.Ships.Statuses.StatusValueFilter

Only lets a rule apply while the named status on the part sits between `MinValue` and `MaxValue`.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## MinValue
`float` · optional · default `-∞`

The lowest combined status value that still counts as a match, inclusive.

## MaxValue
`float` · optional · default `∞`

The highest combined status value that still counts as a match, inclusive.

## StatusType
`→ StatusType` · optional

The status whose combined value at the location is tested. A location with no instance of this status never matches, whatever `MinValue` and `MaxValue` say.
