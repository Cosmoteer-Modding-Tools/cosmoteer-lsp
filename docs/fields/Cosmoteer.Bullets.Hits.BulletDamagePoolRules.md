# Cosmoteer.Bullets.Hits.BulletDamagePoolRules

Gives the bullet a budget of damage that hit effects draw from until it runs out, so a penetrating shot weakens as it goes.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Damage
`number` · required

Damage capacity of the pool, scaled by the projectile's effect scale. `Damage` hit effects that reference the pool drain it as they deal damage, other components can read the remaining fraction through their `FactorEffectsWith` field, and a `DeathByDamagePool` component can destroy the projectile once the pool is empty. The overclocked large flak field uses a 400 point pool shared by its bullet hits and beam attenuation.
