# Cosmoteer.Generators.Simulation.ResourceTypeLoadoutRules

Stores rules controlling how a spawned ship's resource loadout will be determined for a particular type of resource.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ResourceType
`→ ResourceRules` · required

The resource dropped into a flexible storage tile when this entry wins the loadout lottery.

## RandomWeight
`float` · optional · default `1`

Relative weight of this entry in the lottery, which is rolled once for every unpinned flexible storage tile on the ship. Weights only mean anything relative to the other entries of the same `ResourceLoadout`.

## Quantity
`range<int>` · optional

How many units go into the tile, drawn uniformly inclusive and then clamped to the resource's own maximum stack size. It defaults to the largest possible integer at both ends, so tiles are filled right up to the stack limit.
