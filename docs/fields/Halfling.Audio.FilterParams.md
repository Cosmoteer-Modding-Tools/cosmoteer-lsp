# Halfling.Audio.FilterParams

Contains parameters that specify how a sound instance or audio group should be filtered.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## FilterType
`enum FilterType` · optional · one of: `LowPass`, `LowPassAmplify`, `HighPass`, `HighPassAmplify`, `BandPassAmplify`, `Notch`

Specifies whether and how frequencies of audio will be filtered.

## Frequency
`int` · optional

Specifies the cutoff frequency used by the filter.

## QFactor
`float` · optional

Specifies the Q factor used by the filter. This value controls how rapidly the audio will be reduced. It is not used by the LowPass or HighPass filters.
