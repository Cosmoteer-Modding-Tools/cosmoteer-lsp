# Halfling.Gui.Components.Input.WidgetScrollWheelController

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## IncrementAmount
`float` · optional

The amount by which the value will be incremented or decremented when using the scroll wheel.

## EnableSmoothScrolling
`bool` · optional

If true, scrolling will happen smoothly over multiple frames instead of instantly on every click of the scroll wheel.

## SmoothVelocityFraction
`float` · optional

The smooth scrolling velocity (in units per second) as a fraction of the delta between the current value and target value.

## MinSmoothVelocity
`float` · optional

The minimum smooth scrolling velocity (in units per second) regardless of the delta between the current value and target value.

## DisableInputHandling
`bool` · optional

Whether the component's input handling should be skipped.
