# Cosmoteer.Ships.Parts.Logic.PartTileLineScoreValueRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Line
`TileLine` · required

<!-- TODO: needs documentation -->

## BlockingPartIDs
`→ PartRules[]` · optional

<!-- TODO: needs documentation -->

## BlockingPartCategories
`→ PartCategory[]` · optional

<!-- TODO: needs documentation -->

## ZeroScoreTilesBlock
`bool` · optional · default `true`

<!-- TODO: needs documentation -->

## EmptyTilesBlock
`bool` · optional

<!-- TODO: needs documentation -->

## Falloff
`float` · optional

<!-- TODO: needs documentation -->

## OcclusionCheckInterval
`Time` · optional

The time interval between each line occlusion check and subsequent application of effects.

## OccludedByShields
`bool` · optional

Whether other ships' shield arcs occlude the line.

## OcclusionHitEffects
`MultiHitEffectRules` · optional

The hit effects applied to other ships intersecting a line.

## OcclusionContinuousMediaEffects
`MultiMediaEffectRules` · optional

The media effects played when other ships intersect a line.

## EffectScaleFactor
`→ PartComponentRules` · optional

The component providing the value that the occluded score is multiplied by to calculate the effect scale.

## MediaIntensity
`number` · optional

The value passed to media effects as intensity. Defaults to the effect scale if null.

## CustomShapeMediaEffects
`MultiMediaEffectRules` · optional

<!-- TODO: needs documentation -->

## CustomShapeValueSource
`→ PartComponentRules` · optional

<!-- TODO: needs documentation -->

## BlueprintMaterial
`Material` · optional

<!-- TODO: needs documentation -->

## BlueprintLineWidthScale
`float` · optional · default `1`

<!-- TODO: needs documentation -->

## ValueTextFormatKey
`string` · optional

<!-- TODO: needs documentation -->

## TextValueScaleFactor
`float` · optional · default `1`

<!-- TODO: needs documentation -->

## FractionalTextValueBasis
`float` · optional

If not null, the line score as a fraction of this value is passed as a secondary text value.

## FractionalTextValueScaleFactor
`float` · optional · default `1`

The value that the secondary text value is scaled by.
