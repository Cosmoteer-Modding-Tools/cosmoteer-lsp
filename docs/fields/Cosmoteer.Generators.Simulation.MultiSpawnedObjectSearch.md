# Cosmoteer.Generators.Simulation.MultiSpawnedObjectSearch

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## Mode
`enum SpawnedObjectSearchMode` · optional · one of: `First`, `Latest`, `Nearest`, `Random`

How the spawned objects will be searched for.

## Tag
`→ SimObjectSpawner` · required

The tag of the spawned objects to search for.

## Max
`int` · optional

The maximum number of spawned objects to find.

## LimitToBatch
`bool` · optional

Whether only the spawned objects belonging to the same batch as the first spawned object will be found.

## LimitToFleet
`bool` · optional

Whether only the spawned objects belonging to the same fleet as the first spawned object will be found.

## Distance
`float` · optional

The maximum distance that the spawned objects can be from the search location.

## OnlyFactions
`→ FactionRules[]` · optional

If not null, only objects belonging to these factions will be found.

## OnlyOpposingFactions
`bool` · optional

Whether only objects with opposing factions should be returned.
