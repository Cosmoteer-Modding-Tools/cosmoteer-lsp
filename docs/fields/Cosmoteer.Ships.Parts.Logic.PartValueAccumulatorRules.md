# Cosmoteer.Ships.Parts.Logic.PartValueAccumulatorRules

> Machine-generated skeleton. Write prose under each `## Field` heading; the heading and the
> type-signature line below it are regenerated by the scaffolder — don't hand-edit them. Add
> `[[links]]` and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> These docs describe individual fields. For guides, worked examples and the bigger picture, see
> the [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## AccumulatingToggle
`→ PartComponentRules` · optional

The IPartComponentToggle that determines the operational state of the accumulator.

## BaseValue
`float` · optional

The value the accumulator starts at and moves towards when toggle off.

## AccumulationRate
`number` · optional

The rate at which the value accumulates when toggled on.

## DissipationRate
`number` · optional

The rate at which the value moves towards BaseValue when toggled off.

## NonOperationalBehaviour
`enum AccumulatorBehaviour` · optional · one of: `Nothing`, `Dissipate`, `Reset`

The accumulator's behaviour while it is non-operational.

## MinValue
`number` · optional

The lowest value that the accumulator can reach.

## MaxValue
`number` · optional

The highest value that the accumulator can reach.

## AccumulationTriggers
`AccumulationTriggerRules[]` · optional

Discrete value modification triggers.
