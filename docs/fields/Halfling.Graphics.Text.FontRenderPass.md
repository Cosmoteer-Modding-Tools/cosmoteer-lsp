# Halfling.Graphics.Text.FontRenderPass

Describes a single font rendering pass.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## SizeRelativeOffset
`Vector2` · optional

The position offset of this pass relative to the pixel size of the font.

## Effects
`IFontEffect[]` · optional

A modifiable list of effects to apply to the font during this render pass. The effects will be applied in the order in which they appear in this list. Adding or removing effects only has an effect on characters that have not yet been pre-rendered.
