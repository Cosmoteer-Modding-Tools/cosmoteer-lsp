# Halfling.Particles.Updaters.ParticleUvAnimation

Plays a sprite sheet as a flipbook on each particle, stepping frames at `Framerate` as the particle ages.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## TimeIn
`ParticleDataID` · optional

The data that stores the particle's elapsed time.

## UvRectOut
`ParticleDataID` · optional

The data that stores the particle's UV rect coordinates.

## Framerate
`float` · optional

The number of frames displayed every second.

## TextureSize
`IntVector2` · optional

The size of the texture containing the frames of animation.

## FrameSize
`IntVector2` · optional

The size of an individual sprite frame in the texture.

## FrameCount
`int` · optional

The number of sprite frames in the animation.

## FramesPerRow
`int` · optional

The number of sprite frames in a single row in the texture.

## PixelOffset
`IntVector2` · optional

The offset in pixels of the animation frames in the texture.

## Looping
`bool` · optional

Whether the animation should loop after reaching the end. If false, the last frame of animation will be held.
