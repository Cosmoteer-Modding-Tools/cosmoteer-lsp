# Cosmoteer.Ships.Parts.Logic.PartPolledValueRules

Reads another component's value every tick and announces changes, for sources that never report changes themselves.

> Machine-generated skeleton. The line above is the class summary, one sentence saying what this
> class is. Write prose under each `## Field` heading below. The headings and the type-signature
> lines are regenerated by the scaffolder, so a hand-edit to them is overwritten. Add `[[links]]`
> and Markdown freely in the prose. See [README](./README.md) to contribute.
>
> For guides, worked examples and the bigger picture, see the
> [Cosmoteer modding wiki](https://cosmoteer.wiki.gg/wiki/Modding).

## ValueSource
`ComponentValueReferenceRules` · required

The value component polled once per fixed simulation tick while the component is operational, with its value republished to listeners on change. This lets components that need change notifications read lazily evaluated values such as timers. Written either as a bare component ID or as a group with `ID` and an optional `ValueID`.

## DefaultValue
`float` · optional

The value reported before the first poll and restored whenever the component becomes non-operational. The vanilla tractor beam uses 0 so its overload charge progress reads zero while the overload toggle is off.
