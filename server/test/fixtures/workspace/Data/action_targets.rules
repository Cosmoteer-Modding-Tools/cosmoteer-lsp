// Fixture for mod-action TARGET-shape validation tests: a group, a list and a leaf value
// so AddMany (needs a list) and AddBase (needs a list or group) can be exercised.
Group
{
	Leaf = 1
}

List
[
	1
	2
]

Value = 5

// A list of entries, a second list of the same kind of entries, and a list of lists, so the
// check for a list appended as one entry can be exercised in both directions.
Entries
[
	{ A = 1 }
]

MoreEntries
[
	{ B = 2 }
]

Nested
[
	[1]
]
