// A whole-file spawner fragment the manifest appends to a `list<SimSpawner>` slot. The slot names a
// registry, so the file's own `Type = Ships` says which member of it this is and everything below
// reads through ShipSpawner.
Type = Ships
Count = [1, 3]
Retries = 4
SpawnCrew = true
