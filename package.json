{
    "name": "cosmoteer-language-server",
    "displayName": "Cosmoteer Language Server",
    "description": "A complete language server for Cosmoteer rules",
    "author": "Cosmoteer Modding Tools",
    "license": "MIT",
    "version": "0.6.1",
    "repository": {
        "type": "git",
        "url": "https://github.com/Cosmoteer-Modding-Tools/cosmoteer-lsp"
    },
    "publisher": "TrustNoOneElse",
    "categories": [
        "Other",
        "Programming Languages"
    ],
    "keywords": [
        "multi-root ready"
    ],
    "engines": {
        "vscode": "^1.100.0"
    },
    "activationEvents": [
        "workspaceContains:**/*.rules"
    ],
    "pricing": "Free",
    "icon": "./images/Language_Server_Icon.png",
    "main": "./out/client/src/extension.mjs",
    "galleryBanner": {
        "color": "#007ACC",
        "theme": "dark"
    },
    "l10n": "./l10n",
    "contributes": {
        "commands": [
            {
                "command": "cosmoteer.previewShader",
                "title": "Cosmoteer: Preview Shader",
                "category": "Cosmoteer"
            },
            {
                "command": "cosmoteer.showModOverview",
                "title": "Cosmoteer: Show Mod Overview",
                "category": "Cosmoteer"
            },
            {
                "command": "cosmoteer.editPartGrid",
                "title": "Cosmoteer: Edit Part Grid",
                "category": "Cosmoteer"
            },
            {
                "command": "cosmoteer.migrateMod",
                "title": "Cosmoteer: Migrate Mod to Current Game Version",
                "category": "Cosmoteer"
            },
            {
                "command": "cosmoteer.buildModSchemaFromMods",
                "title": "Cosmoteer: Rebuild Schema from Code Mod Assemblies",
                "category": "Cosmoteer"
            },
            {
                "command": "cosmoteer.extractSharedBaseFiles",
                "title": "Cosmoteer: Extract Shared Base Files",
                "category": "Cosmoteer"
            }
        ],
        "menus": {
            "commandPalette": [
                {
                    "command": "cosmoteer.previewShader",
                    "when": "editorLangId == rules"
                },
                {
                    "command": "cosmoteer.showModOverview",
                    "when": "editorLangId == rules"
                },
                {
                    "command": "cosmoteer.editPartGrid",
                    "when": "editorLangId == rules"
                }
            ],
            "editor/context": [
                {
                    "command": "cosmoteer.previewShader",
                    "when": "editorLangId == rules",
                    "group": "navigation@9"
                },
                {
                    "command": "cosmoteer.editPartGrid",
                    "when": "editorLangId == rules",
                    "group": "navigation@10"
                }
            ]
        },
        "languages": [
            {
                "icon": {
                    "dark": "./images/Icon_Dark_Mode.png",
                    "light": "./images/Icon_Light_Mode.png"
                },
                "id": "rules",
                "extensions": [
                    ".rules"
                ],
                "aliases": [
                    "Cosmoteer Rules"
                ]
            },
            {
                "id": "cosmoteer-shader",
                "extensions": [
                    ".shader"
                ],
                "aliases": [
                    "Cosmoteer Shader"
                ],
                "configuration": "./language-configuration.shader.json"
            }
        ],
        "grammars": [
            {
                "language": "rules",
                "scopeName": "source.rules",
                "path": "./syntaxes/rules.tmLanguage.json"
            },
            {
                "language": "cosmoteer-shader",
                "scopeName": "source.cosmoteer-shader",
                "path": "./syntaxes/shader.tmLanguage.json"
            }
        ],
        "semanticTokenScopes": [
            {
                "language": "rules",
                "scopes": {
                    "property": [
                        "variable.name"
                    ],
                    "variable": [
                        "entity.name.tag"
                    ],
                    "enumMember": [
                        "string.unquoted"
                    ],
                    "number": [
                        "constant.numeric"
                    ],
                    "keyword": [
                        "constant.language"
                    ],
                    "function": [
                        "support.function"
                    ],
                    "type": [
                        "entity.name.type"
                    ],
                    "operator": [
                        "keyword.operator"
                    ],
                    "string": [
                        "string.quoted"
                    ]
                }
            }
        ],
        "configuration": {
            "type": "object",
            "title": "Cosmoteer Rules Configurations",
            "properties": {
                "cosmoteerLSPRules.maxNumberOfProblems": {
                    "scope": "resource",
                    "type": "number",
                    "default": 100,
                    "description": "Controls the maximum number of problems produced by the server."
                },
                "cosmoteerLSPRules.trace.server": {
                    "scope": "window",
                    "type": "string",
                    "enum": [
                        "off",
                        "messages",
                        "verbose"
                    ],
                    "default": "off",
                    "description": "Traces the communication between VS Code and the language server."
                },
                "cosmoteerLSPRules.cosmoteerPath": {
                    "scope": "application",
                    "type": "string",
                    "default": "",
                    "description": "The path to the Cosmoteer installation. If not set, the extension cannot validate .rules files correctly. Example: C:/Program Files (x86)/Steam/steamapps/common/Cosmoteer"
                },
                "cosmoteerLSPRules.ignorePaths": {
                    "scope": "application",
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "default": [],
                    "description": "Paths to ignore when validating references. Example: [\"SW_\"]"
                },
                "cosmoteerLSPRules.diagnostics.validateWholeWorkspace": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Validate the whole mod, not just the files open in the editor, so the Problems panel describes the project rather than your tabs. Which files are covered is set by 'workspaceValidationScope'. Results are cached on disk per file, so only the first open of a project pays for the scan. Turn off on a low-memory machine: the pass holds every scanned file's AST while it runs."
                },
                "cosmoteerLSPRules.diagnostics.workspaceValidationScope": {
                    "scope": "resource",
                    "type": "string",
                    "enum": [
                        "allFiles",
                        "modRulesReachable"
                    ],
                    "enumDescriptions": [
                        "Validate every .rules file under the workspace folder(s).",
                        "Validate only the files reachable from the mod.rules manifest: its action sources, their includes and inheritance, and the strings folder. Files open in the editor always validate."
                    ],
                    "default": "modRulesReachable",
                    "description": "Which files the whole-workspace validation pass covers (only relevant when 'validateWholeWorkspace' is on). The default follows the mod.rules actions, so backups, templates and other content the game never loads stay out of the Problems panel. A workspace with no manifest is unrestricted either way."
                },
                "cosmoteerLSPRules.diagnostics.validateComponentReferences": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Flag a component ID reference (e.g. 'OperationalToggle = IsOperational') whose id names no component in the part, its inherited bases, or its include-valued components blocks. Runs only once the game install is indexed."
                },
                "cosmoteerLSPRules.diagnostics.validateCrossFileReferences": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Flag a cross-file ID reference (a GUI toggle/color/targeter/trigger id) whose id names no declaration of that kind in the project or the game data. Runs only once the game install is indexed."
                },
                "cosmoteerLSPRules.diagnostics.validateRequiredFields": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Flag a group that is missing a schema-required field. Fields supplied through the inheritance chain count as present."
                },
                "cosmoteerLSPRules.diagnostics.validateShaderConstants": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Flag an inline shader constant a material sets (e.g. '_hotColor = ...') that the referenced .shader declares no uniform for, or whose value does not match the uniform's type. Runs only when the shader file resolves on disk."
                },
                "cosmoteerLSPRules.diagnostics.validateShaderCode": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Run diagnostics on .shader files: an #include whose target does not exist, a '_'-prefixed uniform that no file in the include chain declares, and a call to a function that is neither an HLSL intrinsic nor defined in scope. The undeclared-symbol checks run only when the whole include chain is readable."
                },
                "cosmoteerLSPRules.diagnostics.validateLocalizationKeys": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Flag a localization key (e.g. 'NameKey = \"Parts/Foo\"') that no language strings file declares. Only literal key paths are checked and matching is case-insensitive, like the game. Runs only once the game install is indexed. Offers a 'did you mean' fix and an insert-into-your-strings-files fix."
                },
                "cosmoteerLSPRules.diagnostics.validateRedundantSeparators": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Hint at a ',' or ';' separator that a line break already makes redundant. The game ends every field or list element at a line break, so separators are only needed between entries on the same line. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.diagnostics.validateIgnoredFields": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Hint at a field the game ignores: its group's schema class does not declare the field and no reference in the file reads it, so the value is never loaded. Offers a remove quick fix. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.diagnostics.validateUnclosedComments": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Warn about a block comment the game does not close. The game closes a block comment only when the run of '*' before the closing '/' is odd, so a banner such as '/****** Section ******/' swallows everything up to the next '*/' when the mod loads. Offers a fix that drops one '*'."
                },
                "cosmoteerLSPRules.diagnostics.validateDefaultValues": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Fade a field that restates the game's own default, so deleting it changes nothing. Only checked inside groups that do not inherit, since an explicitly written default can deliberately override a base's value. Offers a remove quick fix. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.diagnostics.validateUnusedConstants": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Fade a SCREAMING_CASE constant that nothing reads, and a chain of constants that only read each other without ever reaching a field. Only judged when no other file in the project spells the name. Offers a remove quick fix. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.diagnostics.validateDuplicateFields": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Hint at a group whose fields several other files of the same mod write word for word, which could live in one shared base file all of them inherit, the way the game's own data is written. The hint carries the extract-a-shared-base fix, so turning it off removes both. The 'Cosmoteer: Extract Shared Base Files' command keeps working either way. Costs one read of the mod's other files, kept until they change. Only files of the mod being edited are ever rewritten, never the game's own Data folder. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.diagnostics.validateRedundantOverrides": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Fade a field whose value the group already inherits from its base, so writing it leaves the game exactly where deleting it would. Offers a remove quick fix. Only judged when the whole inheritance chain could be read and neither copy carries a reference that would mean something else from where it is written. Shown as an editor hint, not in the Problems panel."
                },
                "cosmoteerLSPRules.allowEditingVanillaFiles": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": false,
                    "description": "Let refactorings read and rewrite the Cosmoteer game Data install. Rename reaches into it, and the shared-base extraction treats it as a project of its own, which it cannot do otherwise because the game tree carries no mod manifest: with this on, the game's own duplicated fields are hinted at and 'Cosmoteer: Extract Shared Base Files' will create base files inside the install and rewrite the files that inherit them. Only turn it on if you are working on the game data itself. Installed workshop mods stay off limits either way."
                },
                "cosmoteerLSPRules.codeMods.enabled": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Understand mods that ship a .dll: the types, fields and 'Type=' values the assembly declares are read out of it and merged into the schema, so a modded component completes, hovers and validates like a built-in one. Covers the open workspace, the installed workshop mods and your own Mods folder. Turn off to use the shipped schema only, at the price of every type such a mod adds being reported as unknown."
                },
                "cosmoteerLSPRules.codeMods.autoRefresh": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Pick up a code mod that is installed, updated or rebuilt while the editor is open, by watching the assemblies the schema was built from. Costs nothing while nothing changes; each change re-scans the mod folders. Turn off to only update on 'Cosmoteer: Rebuild Schema from Code Mod Assemblies'."
                },
                "cosmoteerLSPRules.inlayHints.showBaseValue": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Show the referenced group's BaseValue as an inline hint. A reference like 'Arc = &~/Part/Components/ArcShield/Arc' whose target group has a BaseValue member renders '/BaseValue = 160d' after the reference."
                },
                "cosmoteerLSPRules.associateShaderFiles": {
                    "scope": "window",
                    "type": "boolean",
                    "default": true,
                    "description": "Open .shader files with the Cosmoteer Shader language (HLSL) even when another extension, such as the built-in Unity ShaderLab support, also claims the .shader extension. Only reassigns files opened as ShaderLab or plain text, never a language you chose yourself."
                },
                "cosmoteerLSPRules.decompiler.showInHover": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": false,
                    "description": "Power user: end schema hovers with an 'Open <Class> in decompiler' link that opens the owning C# class from the game's assemblies (Cosmoteer.dll / HalflingCore.dll) in your .NET decompiler. ILSpy and dotPeek installs are found automatically, so 'cosmoteerLSPRules.decompiler.executablePath' is only needed when that fails."
                },
                "cosmoteerLSPRules.decompiler.executablePath": {
                    "scope": "machine-overridable",
                    "type": "string",
                    "default": "",
                    "description": "Path to your .NET decompiler executable, e.g. 'C:\\Tools\\ILSpy\\ILSpy.exe'. Only needed when the automatic search (PATH, winget, Program Files, JetBrains Toolbox, /Applications) does not find your ILSpy or dotPeek install."
                },
                "cosmoteerLSPRules.decompiler.tool": {
                    "scope": "machine-overridable",
                    "type": "string",
                    "enum": [
                        "auto",
                        "ilspy",
                        "dotpeek"
                    ],
                    "default": "auto",
                    "enumDescriptions": [
                        "Infer from the executable's file name.",
                        "ILSpy: launched as '<assembly> /navigateTo:T:<Class>'.",
                        "dotPeek: launched as '/select=<assembly>!T:<Class>'."
                    ],
                    "description": "Which command-line style the 'Open in decompiler' link uses. 'auto' infers ILSpy or dotPeek from the executable's file name."
                },
                "cosmoteerLSPRules.formatting.enabled": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": true,
                    "description": "Enable document formatting (Format Document) for .rules and .shader files. Formatting changes whitespace only; if the result would not lex to the identical token stream, the file is left unchanged."
                },
                "cosmoteerLSPRules.formatting.formatOnSave": {
                    "scope": "resource",
                    "type": "boolean",
                    "default": false,
                    "description": "Format .rules and .shader files before every save, independent of the editor's own 'editor.formatOnSave'. On-save formatting indents with tabs."
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run package",
        "package": "npm run check-types && npm run lint && node esbuild.mjs --production",
        "compile": "node esbuild.mjs",
        "esbuild": "node esbuild.mjs",
        "esbuild-watch": "node esbuild.mjs --watch",
        "watch": "node esbuild.mjs --watch",
        "compile:tsc": "tsc -b",
        "watch:tsc": "tsc -b --noEmit --watch",
        "watch:esbuild": "node esbuild.mjs --watch",
        "watch:esbuild-test": "node esbuild.mjs --watch --test",
        "lint": "eslint ./client/src ./server/src",
        "postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
        "test": "sh ./scripts/e2e.sh",
        "test:server": "cd server && npm test",
        "check-types": "tsc -b --noEmit",
        "check-types:test": "cd server && tsc -p test/tsconfig.json",
        "docs:scaffold": "node tools/docsgen/docsgen.mjs scaffold",
        "docs:compile": "node tools/docsgen/docsgen.mjs compile",
        "docs:lint": "node tools/docsgen/docsgen.mjs lint",
        "l10n": "npx @vscode/l10n-dev export --outDir ./l10n .\\server"
    },
    "overrides": {
        "diff": "^8.0.4",
        "serialize-javascript": "^7.0.7"
    },
    "devDependencies": {
        "@eslint/js": "^10.0.1",
        "@types/mocha": "^10.0.10",
        "@types/node": "^24",
        "@vscode/l10n-dev": "^0.0.35",
        "@vscode/test-electron": "^3.1.0",
        "esbuild": "^0.28.1",
        "eslint": "^10.8.0",
        "eslint-config-prettier": "^10.1.8",
        "glob": "^13.0.6",
        "mocha": "^11.7.6",
        "prettier": "3.9.6",
        "typescript": "^6.0.3",
        "typescript-eslint": "^8.65.0"
    }
}